# Spoolman User Stories — Usage, Importance & Coverage Analysis

This document catalogs what people realistically want to do with Spoolman NG and how well the current codebase (July 2026) serves each need. **Frequency** and **Importance** are heuristic estimates for a typical self-hosting 3D-printing hobbyist; a print farm or makerspace shifts several ratings upward (usage tracking, multi-user, monitoring). **Coverage** ratings are grounded in the fork's source, docs, and its own self-assessments (FORK_ASSESSMENT.md, MASTERPLAN.md). The value of the table is the final column: where the gaps are and what would close them.

**Legend**
- **Frequency:** Every print · Daily · Weekly · Monthly · Occasional · Setup (one-time) · Rare/emergency
- **Importance:** Critical · High · Medium · Low
- **Coverage:** Excellent · Good · Partial · Poor · Missing

| # | Category | User story | Frequency | Importance | How well served today | What could be improved |
|---|---|---|---|---|---|---|
| 1 | Core inventory | As a hobbyist, I want to register a new spool with its starting weight and price so that I know what I own and paid. | Weekly | Critical | Excellent — form prefills weights from filament, three weight-entry modes, currency-aware parsing. | Apply the vendor default tare automatically and clarify which spool_weight source is used. |
| 2 | Core inventory | As a hobbyist, I want to define a filament type once with material, color, price, and temps so that I reuse it across spools. | Weekly | Critical | Excellent — density and diameter mandatory to drive length-to-weight math; specs reused everywhere. | — |
| 3 | Core inventory | As a hobbyist, I want to import filament specs from community databases so that I do not retype spec sheets. | Monthly | High | Excellent — SpoolmanDB, TigerTag, and 3DFP imports autofill fields and auto-create vendors. | 3DFP is a brittle HTML scrape and SpoolmanDB is a frozen snapshot needing upstream sync. |
| 4 | Core inventory | As a hobbyist, I want to archive an empty spool instead of deleting it so that my active list stays clean but history survives. | Weekly | High | Good — archive and unarchive with greyed rows and a show-archived toggle. | Auto-archive at zero grams skips confirmation; add an undo toast. |
| 5 | Core inventory | As a print-farm operator, I want to organize spools into named locations by drag-and-drop so that the app mirrors my shelves. | Weekly | High | Good — Trello-style Kanban board with live-persisted per-location ordering and instant relocation. | Allow dropping a spool anywhere on a populated location, not only onto its cards. |
| 6 | Core inventory | As a print-farm operator, I want to add many similar spools at once via clone or a quantity stepper so that restocking is fast. | Weekly | Medium | Partial — quantity stepper fires N creates fire-and-forget with no per-item feedback. | Await creates with progress and partial-failure reporting; add varied bulk import. |
| 7 | Core inventory | As a hobbyist, I want to record multi-color or gradient filament with its layout so that swatches and icons look right. | Monthly | Medium | Good — up to 14 colors with coaxial or longitudinal blend direction. | — |
| 8 | Core inventory | As a print-farm operator, I want to track vendor SKU and lot numbers so that I can reorder and trace quality issues. | Weekly | Medium | Good — filterable autocompleted article-number and lot-number fields. | Add batch-level rollup to view all spools from one lot across filaments. |
| 9 | Core inventory | As a hobbyist, I want to record a vendor once with its default empty-spool weight so that new spools prefill faster. | Setup (one-time) | Medium | Partial — vendor empty_spool_weight is captured but never consumed by any form. | Actually prefill spool tare weight from the vendor default. |
| 10 | Core inventory | As a hobbyist, I want free-text notes on a spool or filament so that I remember quirks like warping or drying. | Weekly | Low | Partial — notes stored and URLs linkified, but not searchable in UI or API. | Expose comment search and filtering in both the client and the API. |
| 11 | Core inventory | As a print-farm operator, I want to retire a discontinued filament type without breaking referencing spools so that history stays intact. | Occasional | Low | Poor — no archive for filaments or vendors; delete blocked by a generic FK error. | Add an archive flag for filaments and vendors and explain FK-blocked deletes. |
| 12 | Printing workflow & usage tracking | As a hobbyist, I want to record filament used after a print by length or weight so that remaining weight stays accurate. | Every print | Critical | Excellent — Adjust modal deducts length or weight, converted server-side via real cylinder-volume math. | — |
| 13 | Printing workflow & usage tracking | As a hobbyist, I want to put a spool on a scale and enter its gross weight so that Spoolman computes usage. | Weekly | High | Excellent — measured-weight mode derives used weight from initial plus tare minus reading. | Gating depends on filament spool_weight; surface why the mode is disabled. |
| 14 | Printing workflow & usage tracking | As a user with the web UI open, I want remaining weight to update live during a print so that I need not refresh. | Every print | Medium | Good — websocket liveProvider patches rows in place with a weight_delta on each use. | Package the websocket client so custom dashboards can reuse it. |
| 15 | Printing workflow & usage tracking | As a maker with a load-cell scale under the holder, I want to POST a raw weight so that usage auto-computes. | Daily | Medium | Good — PUT measure computes the delta from one gross reading. | Ship reference ESPHome or Arduino firmware or an MQTT bridge; none exists. |
| 16 | Printing workflow & usage tracking | As a hobbyist, I want to see how much filament I used per print and over weeks so that I can budget. | Weekly | Medium | Missing — no usage-history table; per-use deltas are ephemeral websocket payloads only. | Persist a per-use event log to power in-app usage-over-time charts. |
| 17 | Printing workflow & usage tracking | As an OrcaSlicer user, I want to log calibration results per filament so that I can find them next time. | Occasional | Low | Good — calibration sessions and steps modeled, but manually entered, not slicer-integrated. | Add slicer import or export so results are not hand-typed. |
| 18 | Search, organization & UI | As a user with hundreds of spools, I want a free-text search box so that I find items without knowing exact values. | Daily | High | Poor — no search box; backend search and color-match exist but the client never sends them. | Wire the combined search and color-similarity params into the list toolbars. |
| 19 | Search, organization & UI | As a user, I want to filter built-in numeric fields by range so that I can triage low-stock or cheap spools. | Weekly | Medium | Poor — only custom numeric fields get range filters; built-ins are sort-only. | Add min and max range filters to price, weight, length, and temperature columns. |
| 20 | Search, organization & UI | As a small team, I want to select many spools and archive, relocate, or edit them together so that cleanup is not one-by-one. | Weekly | Medium | Missing — no row selection or bulk actions; multi-select exists only for print sheets. | Add multi-row selection with bulk archive, relocate, delete, and field edit. |
| 21 | Search, organization & UI | As a hobbyist opening the app, I want an at-a-glance inventory summary so that I need not dig through lists. | Daily | Medium | Good — KPI tiles, low-stock, material and vendor breakdowns, recently-used, unit-tested aggregation. | Make the hardcoded 15 percent low-stock threshold configurable per user or material. |
| 22 | Search, organization & UI | As a non-English user, I want the UI in my own language so that everything is understandable. | Daily | Medium | Good — 28 locales shipped, also swapping the Ant Design component locale. | Reconnect a translation pipeline; AI-seeded locales are unproofread and drift-prone. |
| 23 | Search, organization & UI | As a user at the printer on a phone, I want list row actions usable on small screens so that I can adjust there. | Daily | Medium | Partial — Actions column hidden below the lg breakpoint, forcing Show pages on phones. | Keep core row actions visible or add a compact mobile action menu. |
| 24 | Search, organization & UI | As a user, I want to customize, persist, and share table views so that a filtered layout is one bookmark away. | Weekly | Low | Excellent — column picker, dark theme, and filters mirrored to localStorage and URL hash. | — |
| 25 | Search, organization & UI | As a user outside metric regions, I want localized currency and units so that numbers make sense. | Daily | Low | Partial — currency is ISO-4217 aware, but weights and lengths are metric-only. | Add imperial ounce, pound, and inch options plus per-field precision. |
| 26 | Labels, QR, NFC & swatches | As a hobbyist, I want to design and print QR label sheets with custom paper, grid, and margins so that labels fit my printer. | Monthly | High | Excellent — full sheet designer with presets, skip-N, copies, live preview, PNG export. | Document the label template escaping rules for literal braces. |
| 27 | Labels, QR, NFC & swatches | As a user at the printer, I want to scan a spool QR with my phone or webcam so that I jump straight to it. | Daily | High | Good — global camera scanner with clear permission and HTTPS error messaging. | Support generic 1D and 2D barcodes so vendor-printed codes also scan. |
| 28 | Labels, QR, NFC & swatches | As a user whose filament has an NFC tag, I want to tap it to find or create the spool so that I skip typing specs. | Weekly | High | Good — TigerTag, OpenPrintTag, and Qidi decoded via Web NFC or a USB reader. | Document supported readers and USB or udev setup; Web NFC needs HTTPS. |
| 29 | Labels, QR, NFC & swatches | As a user, I want to control which fields and text appear on a label so that labels match my convention. | Monthly | Medium | Good — tag template language with conditional blocks, bold runs, and custom fields. | Document the mini-language beyond the in-app tag list. |
| 30 | Labels, QR, NFC & swatches | As a hobbyist with many similar spools, I want to 3D-print a physical color swatch so that I match filament by eye. | Occasional | Medium | Excellent — dual-material 3MF, five styles, WCAG-contrast marking, client-side font and QR. | — |
| 31 | Labels, QR, NFC & swatches | As a user, I want the QR to encode a normal HTTP URL so that any phone camera opens the spool page. | Setup (one-time) | Medium | Good — optional deep-link mode using the configured base URL. | Needs base_url reachable from the scanning device; the default scheme opens only in-app. |
| 32 | Labels, QR, NFC & swatches | As a Klipper user with an NFC reader at the printer, I want it to auto-select the loaded spool so that usage attributes correctly. | Every print | Medium | Poor — the nfc lookup hook exists but no shipped Klipper daemon calls it. | Ship or link a Moonraker component that reads tags and posts lookups. |
| 33 | Printer & ecosystem integrations | As a Klipper user, I want my printer to report consumption automatically so that I never update spool weights by hand. | Every print | Critical | Good — Moonraker's native spoolman component uses PUT use, concurrency-safe, but lives outside this repo. | Add Moonraker contract tests; nothing in-repo verifies the request shape. |
| 34 | Printer & ecosystem integrations | As a Klipper user, I want to pick the loaded spool from Fluidd, Mainsail, or KlipperScreen so that usage attributes right. | Every print | High | Good — frontends read Moonraker via plain GET and PATCH spool plus the websocket. | Get ecosystem docs to point at this fork instead of upstream. |
| 35 | Printer & ecosystem integrations | As an OctoPrint user, I want a plugin to auto-deduct filament like Klipper users get so that tracking is automatic. | Every print | High | Good — community octoprint-spoolman plugin consumes the REST API; nothing in-repo. | Publish compatibility tests and updated docs for the plugin. |
| 36 | Printer & ecosystem integrations | As a Home Assistant user, I want spool weight and low-stock as entities so that I can automate reorder alerts. | Daily | Medium | Good — community custom component polls the REST API; not built or tested here. | Coordinate the HA integration to target this fork; consider native webhooks. |
| 37 | Printer & ecosystem integrations | As a user away from home, I want to manage my spool remotely without exposing Spoolman so that access stays safe. | Weekly | Medium | Good — OctoEverywhere offers a Spoolman page entirely on their side. | Unverifiable from this repo; document the setup and its limits. |
| 38 | Printer & ecosystem integrations | As a user, I want to sync with Bambu Lab or slicer profiles so that I need not re-enter filament data. | Monthly | Medium | Missing — no Bambu Cloud, AMS, PrusaSlicer, or OrcaSlicer software integration exists. | Add slicer profile export and import or a Bambu sync bridge. |
| 39 | Printer & ecosystem integrations | As an AI-assistant user, I want to check stock or log usage conversationally so that I skip the web UI. | Weekly | Low | Good — community spoolman-mcp exposes the REST API over MCP, outside this repo. | Link and version-test the MCP server against this fork's API. |
| 40 | API & automation | As a developer, I want a documented REST API with search, filter, sort, and pagination so that I can build tooling. | Occasional | High | Excellent — full CRUD, extra-field filters, X-Total-Count, Swagger, ReDoc, hosted OpenAPI. | Publish generated client SDKs and trim deprecated query-param aliases. |
| 41 | API & automation | As a developer, I want to subscribe to a change feed so that my tool reacts instantly instead of polling. | Occasional | Medium | Good — hierarchical websocket subscriptions with event-driven pushes on every mutation. | Document the receive-loop quirk; the in-memory tree breaks multi-worker deployments. |
| 42 | API & automation | As a self-hoster, I want to feed metrics into Prometheus and Grafana so that I can alert on low stock and trends. | Setup (one-time) | Medium | Good — opt-in metrics endpoint with 7 gauges and ready-made PromQL examples. | Ship a Grafana dashboard JSON; the endpoint is unauthenticated and refreshes only per minute. |
| 43 | API & automation | As an integrator, I want outbound webhooks or MQTT on events so that external systems react without an always-on subscriber. | Occasional | Medium | Missing — no webhooks, MQTT, or notification push anywhere in the codebase. | Add outbound webhooks or MQTT for low-stock and usage events. |
| 44 | API & automation | As a developer, I want a generated client SDK so that I need not hand-roll HTTP calls against the schema. | Occasional | Low | Poor — no codegen tooling; only the bundled React app and the OpenAPI schema. | Generate and publish typed SDKs from the OpenAPI schema in CI. |
| 45 | Deployment & installation | As a Docker self-hoster, I want a copy-pasteable compose file so that I get running in minutes. | Setup (one-time) | Critical | Excellent — minimal compose plus multi-arch amd64, arm64, and armv7 images with documented tags. | — |
| 46 | Deployment & installation | As a Raspberry Pi user running Klipper, I want a native install with a boot service so that Spoolman survives reboots without Docker. | Setup (one-time) | High | Good — one-line installer, package-manager detection, optional systemd service. | Document armv7 source-build fragility; native install is Linux-only. |
| 47 | Deployment & installation | As a homelab admin, I want to point Spoolman at my Postgres, MySQL, or CockroachDB so that databases are centralized. | Setup (one-time) | Medium | Good — all four DB types are first-class, documented, and CI-tested. | Automatic backups cover SQLite only; external DBs need your own tooling. |
| 48 | Deployment & installation | As an admin behind one reverse proxy, I want to serve Spoolman under a sub-path so that I avoid a dedicated subdomain. | Setup (one-time) | Medium | Good — base-path setting honored end-to-end and covered by e2e tests. | Add forwarded-headers support for correct scheme and client IP behind TLS proxies. |
| 49 | Deployment & installation | As an operator under Docker or Kubernetes, I want a health endpoint so that the platform can gate or restart instances. | Setup (one-time) | Medium | Excellent — health endpoint wired into the image HEALTHCHECK and CI smoke tests. | — |
| 50 | Deployment & installation | As a Windows or macOS user, I want to run Spoolman on my PC so that I need no separate Linux box. | Setup (one-time) | Medium | Partial — supported via Docker only; native install is Linux-only by design. | Document the Docker Desktop path explicitly for Windows and macOS. |
| 51 | Deployment & installation | As a Synology NAS owner, I want documented Container Manager steps so that I can host Spoolman on my NAS. | Setup (one-time) | Medium | Poor — zero Synology or DSM material; generic Docker likely works but is untested and unguided. | Add a Container Manager guide with PUID, PGID, and volume screenshots. |
| 52 | Operations, updating & maintenance | As a self-hoster, I want clear steps to update Spoolman per deployment type so that I get fixes without guesswork. | Monthly | Critical | Partial — docker pull, native reinstall, and Moonraker paths work but update commands are undocumented. | Document the exact docker compose pull and native re-run update sequences. |
| 53 | Operations, updating & maintenance | As an operator, I want migrations to apply automatically and safely when I update so that I run no manual DB commands. | Monthly | High | Good — Alembic upgrades on every startup for all DB types. | Take an automatic pre-migration backup; only a doc warning exists today. |
| 54 | Operations, updating & maintenance | As a self-hoster, I want automatic nightly database backups so that a bad update or disk fault does not wipe inventory. | Daily | High | Good — nightly SQLite backup with 5-file rotation, on by default. | Cover Postgres and MySQL; surface the API-only backup trigger and status in the UI. |
| 55 | Operations, updating & maintenance | As a self-hoster, I want to restore my database after a bad upgrade or mass-delete so that I recover quickly. | Rare/emergency | High | Poor — manual file swap only, SQLite-only, with no endpoint, script, or UI. | Add a guided restore endpoint or CLI with backup validation. |
| 56 | Operations, updating & maintenance | As a Klipper user, I want Spoolman in Mainsail or Fluidd's update list so that upgrading is one click. | Monthly | Medium | Good — release_info.json matches Moonraker's web update_manager via opt-in config. | Klipper-only; releases can lag master, so one-click users run older code. |
| 57 | Operations, updating & maintenance | As a Docker user, I want a loud warning on a mis-configured volume so that I do not lose inventory on recreation. | Setup (one-time) | Medium | Excellent — startup logs a loud repeated warning when the data dir is not mounted. | Surface the warning in the web UI, not only container logs. |
| 58 | Operations, updating & maintenance | As a self-hoster, I want the UI to notify me when a new version is available so that I know to update. | Monthly | Medium | Missing — the version display never checks GitHub releases; only the running version shows. | Add an opt-in upstream release check with an update-available banner. |
| 59 | Operations, updating & maintenance | As a self-hoster, I want a one-click self-update from the app so that I need not remember manual upgrades. | Monthly | Medium | Missing — no self-update path; only Moonraker's opt-in update_manager helps Klipper users. | Offer an in-app or bundled updater for Docker and native installs. |
| 60 | Data management & administration | As a hobbyist, I want to add custom fields to spools and filaments so that I track what matters to me. | Setup (one-time) | High | Excellent — 8 typed field kinds per entity, validated, filterable, and sortable. | Clarify whether stored values survive when a field definition is deleted. |
| 61 | Data management & administration | As a data-conscious hobbyist, I want to export my inventory as CSV or JSON so that I own a portable copy. | Monthly | High | Partial — export endpoints exist but no button appears anywhere in the client. | Add an export button on the list and settings pages. |
| 62 | Data management & administration | As an admin, I want to trigger a backup before a risky operation so that I have a safety net. | Occasional | Medium | Partial — backup endpoint exists but no UI button, and SQLite-only. | Add a backup button and show backup status from the info endpoint. |
| 63 | Data management & administration | As a hobbyist, I want to know my total stash value and cost-per-print so that I understand my spend. | Monthly | Medium | Partial — dashboard shows total value only; no cost-per-print or spend-over-time. | Add cost-per-gram, cost-per-print, and spend-over-time from a usage-event log. |
| 64 | Data management & administration | As a print-farm operator, I want to attribute consumption to a job, printer, or user so that I can bill accurately. | Every print | Medium | Missing — used_weight is one counter with no job, printer, or user linkage. | Add a usage-event log with job, printer, and user tags for reporting. |
| 65 | Data management & administration | As a hobbyist switching to Spoolman, I want to import my existing spools from a spreadsheet so that I skip manual entry. | Setup (one-time) | Medium | Missing — no CSV or JSON import of user data; only catalog and NFC creation. | Add a CSV or JSON import that round-trips the export format. |
| 66 | Security & multi-user | As an admin, I want to control who can read or write inventory when exposed beyond the LAN so that it is not wide open. | Setup (one-time) | High | Missing — no built-in auth by design; reverse proxy or VPN only. | Ship the road-mapped opt-in bearer token for mutating endpoints. |
| 67 | Security & multi-user | As a makerspace admin, I want each member to have their own login so that they only edit their own spools. | Daily | Medium | Missing — no user model exists; the roadmap is a shared token, not accounts. | Add optional user accounts with per-record ownership. |
| 68 | Security & multi-user | As a makerspace admin, I want role-based and read-only access so that new members browse without risking shared inventory. | Daily | Medium | Missing — all-or-nothing API access with no roles or read-only mode. | Add read-only and admin roles, ideally enforced at the API layer. |
| 69 | Security & multi-user | As an operator on a shared network, I want to stop rogue devices from draining spools or writing NFC tags so that data stays safe. | Setup (one-time) | Medium | Missing — unauthenticated writes and the NFC-write endpoint open to anyone on the port. | Add the opt-in API token and rate-limit NFC lookup and auto-create. |

## Top improvement themes

1. **Ship authentication.** Rows 66–69 are all Missing and are the fork's own stated adoption ceiling; start with the road-mapped opt-in bearer token, then optional accounts and read-only/admin roles.
2. **Persist a per-use event log.** One durable usage-history table unlocks in-app usage-over-time (row 16), cost-per-print and spend trends (row 63), and job/printer/user attribution (row 64) — none of which exist today because only a running weight counter is kept (row 63 already surfaces total value, but not these).
3. **Bring data portability into the UI.** Export (row 61), backup trigger (row 62), and restore (row 55) exist only as API calls or manual steps, and import (row 65) is absent; add buttons plus a round-tripping CSV/JSON import.
4. **Close the update lifecycle gap.** Document per-deployment update commands (row 52), add an upstream update-available check (row 58) and a self-updater (row 59), and take an automatic pre-migration backup (row 53).
5. **Make search and bulk editing first-class.** Wire the existing but unused backend free-text and color search into a search box (row 18), add numeric range filters (row 19), and add multi-row bulk actions (row 20).
6. **Write the missing platform and hardware docs.** A Synology Container Manager guide (row 51), NFC reader and HTTPS setup (row 28), and armv7 caveats (row 46) would remove real onboarding friction.
7. **Harden ecosystem integrations.** Add contract tests and redirect Moonraker, Mainsail, Fluidd, OctoPrint, and Home Assistant docs to this fork (rows 33–36), and ship a Grafana dashboard (row 42) and a smart-scale reference bridge (row 15).
8. **Polish two quiet footguns.** Keep row actions usable on phones (row 23) and actually consume the vendor default tare weight that is captured but ignored (row 9).
