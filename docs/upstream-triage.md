# Upstream backlog triage — Donkie/Spoolman → Spoolman NG

**Date:** 2026-07-06 · **Input:** all 265 open issues of the abandoned upstream [Donkie/Spoolman](https://github.com/Donkie/Spoolman) (dump committed as `upstream_issues.json`, PRs excluded) · **Method:** one-time multi-agent sweep — every issue investigated against this fork's current code (56 bug reports by Opus agents, 209 requests by Sonnet agents, topic-batched; every verdict cites `path:line` evidence; duplicates merged by an Opus clustering pass). Completes the MASTERPLAN item "Upstream backlog sweep".

**Result:** 24 FIX · 108 IMPLEMENT · 133 SKIP → **98 fork issues** filed covering 132 upstream issues (duplicates merged: 34).

Skip reasons: already-implemented ×62, already-fixed ×24, out-of-scope ×17, insufficient-info ×9, environment-specific ×9, superseded ×6, support-question ×6.

Verification: 1225/1229 cited code references resolve mechanically to real files/lines; no actionable verdict is low-confidence. Caveat: upstream issue screenshots/comments were unreachable from this environment, so a few visual-symptom verdicts (e.g. #510/#612) rest on code analysis alone.

## Actionable — one fork issue per row

| Fork issue | Kind | Pri | Effort | Covers upstream | Title / investigation summary |
|---|---|---|---|---|---|
| [#44](https://github.com/sherrmann/Spoolman/issues/44) | fix | P1 | S | [#426](https://github.com/Donkie/Spoolman/issues/426) | **Guard useInitialTableState JSON.parse (and add an ErrorBoundary) so corrupt saved state can't blank the list pages** (medium confidence) — The report is a client-side white-screen: clicking Spools or Filaments shows a blank page with browser console errors while the docker container logs are clean, so it is an unhandled JavaScript exception, not a backend 500. The fork's useInitialTableState still calls JSON.parse on persisted sorters/… |
| [#45](https://github.com/sherrmann/Spoolman/issues/45) | fix | P1 | S | [#780](https://github.com/Donkie/Spoolman/issues/780) | **Normalize color_hex in the filament validator so oversized values cannot 500 and brick the filament list** (high confidence) — The color_hex field validator normalizes the value into clr (uppercased, '#' stripped) but returns the original input v at spoolman/api/v1/filament.py:139, so a POST/PATCH with color_hex '#FF000000' passes validation yet is stored with the leading '#' as a 9-character string. spoolman/database/filam… |
| [#47](https://github.com/sherrmann/Spoolman/issues/47) | fix | P1 | S | [#923](https://github.com/Donkie/Spoolman/issues/923) | **Cover fetch() reads and the WebSocket live provider in the 401 auth-reload handler** (high confidence) — The fork already implements the mechanism this issue proposes, but only for one of the three transports it names, so the described hang can still happen. client/src/utils/authReloadHandler.ts:47-53 defines `handleAuthResponseError`, which on a 401 for a GET/HEAD request calls `reloadOnAuthFailure()`… |
| [#49](https://github.com/sherrmann/Spoolman/issues/49) | feature | P1 | M | [#201](https://github.com/Donkie/Spoolman/issues/201), [#478](https://github.com/Donkie/Spoolman/issues/478), [#539](https://github.com/Donkie/Spoolman/issues/539), [#688](https://github.com/Donkie/Spoolman/issues/688), [#742](https://github.com/Donkie/Spoolman/issues/742), [#746](https://github.com/Donkie/Spoolman/issues/746) | **Show spool count per filament (and spool/filament counts per vendor) as list columns** (high confidence) — The request asks the filaments list to show how many spools exist for each filament, so users can see at a glance when they are down to their last spool of a given color/material. Nothing in the current schema carries this: the IFilament interface (client/src/pages/filaments/model.tsx:3-21) has no s… |
| [#51](https://github.com/sherrmann/Spoolman/issues/51) | feature | P1 | M | [#17](https://github.com/Donkie/Spoolman/issues/17), [#356](https://github.com/Donkie/Spoolman/issues/356), [#598](https://github.com/Donkie/Spoolman/issues/598) | **Add free-text search across the spool/filament list pages (matching all fields, including comments and tags)** (medium confidence) — No fuzzy-matching library (e.g. Fuse.js) is present in client/package.json, and the only approximate-match helper in the codebase, `searchMatches` (client/src/utils/filtering.ts:74-77), is a plain case-insensitive check that every whitespace-separated query word is a substring of the target — no typ… |
| [#53](https://github.com/sherrmann/Spoolman/issues/53) | feature | P1 | M | [#265](https://github.com/Donkie/Spoolman/issues/265), [#347](https://github.com/Donkie/Spoolman/issues/347), [#572](https://github.com/Donkie/Spoolman/issues/572) | **Show total remaining weight in stock per filament on the Filament list** (high confidence) — `allColumns` on the Filaments list (client/src/pages/filaments/list.tsx:64-79) has no aggregate stock/weight column, and the Filament show page (client/src/pages/filaments/show.tsx:54-57) only offers a "Spools" button that navigates to a filtered Spool list, requiring the user to manually total `rem… |
| [#55](https://github.com/sherrmann/Spoolman/issues/55) | feature | P1 | M | [#212](https://github.com/Donkie/Spoolman/issues/212), [#566](https://github.com/Donkie/Spoolman/issues/566), [#705](https://github.com/Donkie/Spoolman/issues/705) | **Add CSV/JSON import for spools, filaments, and vendors (round-tripping the existing export format)** (high confidence) — Spoolman NG can export spools/filaments/vendors as CSV or JSON (spoolman/api/v1/export.py:28-71, using spoolman/export.py:41-67's flatten_sqlalchemy_object/dump_as_csv/dump_as_json), but there is no counterpart that reads a CSV/JSON file back into the database: there is no import router, no parser, … |
| [#58](https://github.com/sherrmann/Spoolman/issues/58) | feature | P1 | M | [#669](https://github.com/Donkie/Spoolman/issues/669), [#685](https://github.com/Donkie/Spoolman/issues/685), [#795](https://github.com/Donkie/Spoolman/issues/795) | **Add value-formatting to the label template language (numeric rounding/precision, date/time formatting, per-tag font size)** (high confidence) — The label template engine still has no formatting operators, only substitution. client/src/pages/printing/printing.tsx:63-101 (getTagValue/applyTextFormatting/renderLabelContents) supports exactly four things: {tag} substitution, {prefix{tag}suffix} conditional blocks, **bold** runs, and \n-to-<br> … |
| [#46](https://github.com/sherrmann/Spoolman/issues/46) | feature | P1 | S | [#109](https://github.com/Donkie/Spoolman/issues/109), [#739](https://github.com/Donkie/Spoolman/issues/739) | **Expose the existing color-similarity filter as a UI control on the Filament and Spool lists** (high confidence) — The backend already implements exactly the algorithm the issue suggests: GET /api/v1/filament accepts color_hex and color_similarity_threshold query parameters (spoolman/api/v1/filament.py:290-306,341-346) that call find_by_color, which converts hex colors to CIE-Lab and filters by real Delta-E perc… |
| [#48](https://github.com/sherrmann/Spoolman/issues/48) | feature | P1 | M | [#229](https://github.com/Donkie/Spoolman/issues/229), [#501](https://github.com/Donkie/Spoolman/issues/501) | **Add opt-in bearer-token API authentication (SPOOLMAN_API_TOKEN)** (high confidence) — There is no authentication or authorization mechanism anywhere in the fork's backend: a search for API-token/bearer/auth-middleware patterns across spoolman/*.py returns no hits, and spoolman/main.py:56 and spoolman/main.py:123-124 show the only registered middleware is GZip and CORS, nothing auth-r… |
| [#50](https://github.com/sherrmann/Spoolman/issues/50) | feature | P1 | L | [#751](https://github.com/Donkie/Spoolman/issues/751), [#916](https://github.com/Donkie/Spoolman/issues/916) | **Add a persisted, timestamped per-spool usage/adjustment log** (high confidence) — Spoolman only tracks aggregate usage, not a history: spoolman/database/models.py:66-87 gives Spool only `first_used`/`last_used` timestamps, a running `used_weight` total, and a single free-text `comment` field for the whole spool, with no per-event log table anywhere in the schema. The consumption … |
| [#52](https://github.com/sherrmann/Spoolman/issues/52) | feature | P1 | L | [#775](https://github.com/Donkie/Spoolman/issues/775), [#852](https://github.com/Donkie/Spoolman/issues/852) | **Add optional user accounts with password login and admin/read-only roles** (high confidence) — There is no authentication or user-account system anywhere in the fork's current code: there is no user/login table in spoolman/database/, and a search for "class User", "login", "Login", or "password" in spoolman/ only matches DB-connection password handling (spoolman/database/database.py:29, spool… |
| [#54](https://github.com/sherrmann/Spoolman/issues/54) | feature | P1 | S | [#69](https://github.com/Donkie/Spoolman/issues/69) | **Suggest a default density value based on the entered material** (high confidence) — The `density` field on filament create is a bare required InputNumber with no default and no material-based suggestion (client/src/pages/filaments/create.tsx:287-300: `required: true, type: 'number', min: 0, max: 100`, no onBlur/lookup logic), and `material` itself is a free-text Input with no dropd… |
| [#56](https://github.com/sherrmann/Spoolman/issues/56) | feature | P1 | L | [#217](https://github.com/Donkie/Spoolman/issues/217) | **Add Bambu Lab AMS/Cloud integration to sync spool assignment and usage** (high confidence) — No Bambu Lab integration exists anywhere in the fork: every 'bambu' hit in the codebase is incidental -- a code comment about Bambu Studio's 3MF color-import quirks in client/src/utils/swatch/threeMf.ts:11, a placeholder vendor name in a dashboard benchmark list at client/src/pages/home/analytics.be… |
| [#57](https://github.com/sherrmann/Spoolman/issues/57) | fix | P2 | S | [#728](https://github.com/Donkie/Spoolman/issues/728), [#814](https://github.com/Donkie/Spoolman/issues/814) | **Fix inherited (filament-level) spool price dropping to 0 on the spool list after a live update** (high confidence) — The spool list still drops an inherited (filament-level) price the moment a spool is adjusted via the quick 'Spool Adjustment' button, and the price returns only after a manual reload, exactly as reported. Root cause is an asymmetry between the REST and websocket serializers combined with a too-narr… |
| [#59](https://github.com/sherrmann/Spoolman/issues/59) | fix | P2 | S | [#794](https://github.com/Donkie/Spoolman/issues/794), [#829](https://github.com/Donkie/Spoolman/issues/829) | **Render printed QR codes at a predictable size independent of label text length, and expose QR padding** (high confidence) — The printed QR code still has no fixed size; its rendered dimension varies with the length of the accompanying label text, as reported. In the shared QR label layout each label cell is a flex row ('.print-qrcode-item' with display:flex, client/src/pages/printing/qrCodePrintingDialog.tsx:168-173) con… |
| [#60](https://github.com/sherrmann/Spoolman/issues/60) | fix | P2 | M | [#1](https://github.com/Donkie/Spoolman/issues/1) | **Make PUT /spool/{id}/use idempotent against client retries** (medium confidence) — The `/spool/{spool_id}/use` endpoint (spoolman/api/v1/spool.py:501-536, handler `use()`) still has no idempotency mechanism: `SpoolUseParameters` (spool.py:105-107) only carries `use_length`/`use_weight` floats, with no client-supplied token, nonce, or ETag/If-Match precondition of any kind. Each ca… |
| [#61](https://github.com/sherrmann/Spoolman/issues/61) | fix | P2 | S | [#299](https://github.com/Donkie/Spoolman/issues/299) | **Allow creating/measuring a spool heavier than its theoretical weight without a negative-used-weight error** (high confidence) — The bug is fully reproducible in the current code: when creating a spool via the 'measured weight' entry mode, client/src/pages/spools/create.tsx:452-453 computes used_weight as getGrossWeight() minus the entered value, and if the user's actual measured weight exceeds the theoretical filament.weight… |
| [#62](https://github.com/sherrmann/Spoolman/issues/62) | fix | P2 | S | [#438](https://github.com/Donkie/Spoolman/issues/438) | **Make the /spool/print spool picker table fill the viewport instead of a fixed 400px height** (high confidence) — The spool picker at /spool/print is rendered by SpoolSelectModal, whose table hardcodes a fixed vertical scroll height of 400px at client/src/pages/printing/spoolSelectModal.tsx:132 (scroll={{ x: "max-content", y: 400 }}), so the list stays cramped no matter how tall the browser window is, exactly m… |
| [#63](https://github.com/sherrmann/Spoolman/issues/63) | fix | P2 | M | [#459](https://github.com/Donkie/Spoolman/issues/459) | **Sort text columns (manufacturer/vendor name) case-insensitively** (high confidence) — Sorting the filament list by manufacturer places lowercase-initial vendors (e.g. 'eSUN') below all uppercase-initial ones (e.g. |
| [#64](https://github.com/sherrmann/Spoolman/issues/64) | fix | P2 | S | [#540](https://github.com/Donkie/Spoolman/issues/540) | **Suppress the adjacent newline when a conditional label block resolves to empty** (high confidence) — The label template engine's conditional-block feature drops a block's own text when its tag is unresolved but leaves the surrounding template newline in place, producing a blank output line exactly as reported. renderLabelContents (client/src/pages/printing/printing.tsx:103-128) matches a `{prefix{t… |
| [#65](https://github.com/sherrmann/Spoolman/issues/65) | fix | P2 | S | [#577](https://github.com/Donkie/Spoolman/issues/577) | **Honor extra-field order in filament forms, list columns, and show page** (high confidence) — The extra-field 'order' value is honored only on the Settings management table, which sorts rows by it (client/src/pages/settings/extraFieldsSettings.tsx:591); it is ignored everywhere users actually enter or view data. The backend get_extra_fields returns fields in stored-array (insertion) order an… |
| [#66](https://github.com/sherrmann/Spoolman/issues/66) | fix | P2 | M | [#634](https://github.com/Donkie/Spoolman/issues/634) | **Preserve manually-entered measured weight when empty/initial weight is changed afterward** (medium confidence) — In both the spool create and edit forms the three weight inputs (used / remaining / measured) are all views over a single usedWeight state; the measured-weight field's displayed value is computed as gross weight minus used weight (client/src/pages/spools/create.tsx:205-209 and client/src/pages/spool… |
| [#67](https://github.com/sherrmann/Spoolman/issues/67) | fix | P2 | S | [#671](https://github.com/Donkie/Spoolman/issues/671) | **Reject filament density/diameter of 0 in the form instead of failing with a 422** (high confidence) — The backend requires filament density and diameter to be strictly positive: density: float = Field(gt=0, ...) and diameter: float = Field(gt=0, ...) (spoolman/api/v1/models.py:128-129). The filament create and edit forms, however, validate density with min: 0 (client/src/pages/filaments/create.tsx:2… |
| [#68](https://github.com/sherrmann/Spoolman/issues/68) | fix | P2 | S | [#791](https://github.com/Donkie/Spoolman/issues/791) | **Support running the Docker image as a non-root user (skip usermod/gosu when not root)** (high confidence) — The fork's entrypoint.sh unconditionally runs `groupmod -o -g "$PGID" app` and `usermod -o -u "$PUID" app` (entrypoint.sh:8-9) and then drops privileges with `exec gosu "app" uvicorn ...` (entrypoint.sh:24), with no check for whether the container is actually running as root. The Dockerfile declares… |
| [#69](https://github.com/sherrmann/Spoolman/issues/69) | fix | P2 | M | [#797](https://github.com/Donkie/Spoolman/issues/797) | **Fix JS integer-precision loss on large CockroachDB IDs that makes the UI 404 on vendor_id** (high confidence) — CockroachDB assigns integer primary keys via its default unique_rowid(), producing 64-bit values such as the reported vendor id 1134663890672549889; the fork keeps plain autoincrement Integer primary keys with no explicit sequence (spoolman/database/models.py:18,35,69) and actively supports and CI-t… |
| [#70](https://github.com/sherrmann/Spoolman/issues/70) | fix | P2 | S | [#851](https://github.com/Donkie/Spoolman/issues/851) | **Add External ID field to the filament Create form and the column show/hide list** (high confidence) — The filament Create form omits the External ID field: client/src/pages/filaments/create.tsx goes from article_number (lines 379-390) straight to comment (391-401) with no external_id Form.Item, whereas the Edit form exposes it as an editable input at client/src/pages/filaments/edit.tsx:410-420. The … |
| [#71](https://github.com/sherrmann/Spoolman/issues/71) | fix | P2 | M | [#886](https://github.com/Donkie/Spoolman/issues/886) | **Emit explicit @page size/orientation so labels print correctly on roll/label printers** (medium confidence) — The label print flow still provides no way to control page orientation or page size, so labels laid out in landscape cannot be rotated to print onto a portrait-fed roll/label (e.g. the reporter's Brother QL-500 with 29x90 mm labels). |
| [#72](https://github.com/sherrmann/Spoolman/issues/72) | fix | P2 | M | [#925](https://github.com/Donkie/Spoolman/issues/925) | **Make 'Save as Image' export all labels (single image per page or a zip) instead of N separate downloads** (high confidence) — The 'Save as Image' button still exports each label as a separate browser download in a tight loop, so bulk exports of many labels are silently truncated as reported (user selected 113, got ~10). saveAsImage iterates over every '.print-qrcode-item', and for each one renders a PNG and immediately tri… |
| [#73](https://github.com/sherrmann/Spoolman/issues/73) | feature | P2 | L | [#273](https://github.com/Donkie/Spoolman/issues/273), [#453](https://github.com/Donkie/Spoolman/issues/453), [#734](https://github.com/Donkie/Spoolman/issues/734), [#749](https://github.com/Donkie/Spoolman/issues/749) | **Add multi-row selection with bulk edit/archive/relocate for spools and filaments** (high confidence) — No list page supports multi-row selection or bulk actions: none of client/src/pages/spools/list.tsx, client/src/pages/filaments/list.tsx, or client/src/pages/vendors/list.tsx set an antd `rowSelection` prop on their `<Table>` (a repo-wide search for "rowSelection" across client/src/ returns zero mat… |
| [#74](https://github.com/sherrmann/Spoolman/issues/74) | feature | P2 | L | [#261](https://github.com/Donkie/Spoolman/issues/261), [#473](https://github.com/Donkie/Spoolman/issues/473), [#826](https://github.com/Donkie/Spoolman/issues/826) | **Add an optional per-spool color override that falls back to the filament color** (high confidence) — Color is modeled exclusively on Filament, not Spool: spoolman/database/models.py:51-53 defines color_hex/multi_color_hexes/multi_color_direction only on the Filament table, and the Spool table (spoolman/database/models.py:66-87) has no color column of its own, so today the only way to have two diffe… |
| [#75](https://github.com/sherrmann/Spoolman/issues/75) | feature | P2 | L | [#26](https://github.com/Donkie/Spoolman/issues/26), [#932](https://github.com/Donkie/Spoolman/issues/932) | **Add a Printer entity with spool assignment for per-printer usage attribution** (medium confidence) — There is no Printer entity anywhere in the schema: `models.Spool` (spoolman/database/models.py:66-87) has no printer foreign key, only a free-text `location: Mapped[str \| None] = mapped_column(String(64))` (models.py:79). The only other "printer_name" field in the codebase is `CalibrationSession.pri… |
| [#84](https://github.com/sherrmann/Spoolman/issues/84) | feature | P2 | M | [#90](https://github.com/Donkie/Spoolman/issues/90), [#935](https://github.com/Donkie/Spoolman/issues/935) | **Support printable/scannable location labels with a scan-to-move workflow (label resolves to the location's current spool)** (high confidence) — Locations are not a first-class entity: `Spool.location` is just `String(64)` (spoolman/database/models.py:79), and `GET /location` (spoolman/api/v1/other.py:102-126) simply returns the distinct location strings already present on spools, with `PATCH /location/{location}` (other.py:133-148) doing a … |
| [#85](https://github.com/sherrmann/Spoolman/issues/85) | feature | P2 | M | [#92](https://github.com/Donkie/Spoolman/issues/92), [#355](https://github.com/Donkie/Spoolman/issues/355) | **Add a unit-system setting and apply m/kg auto-scaling across list and show views** (high confidence) — The fork already has exactly the display-formatting logic this issue asks for, tested and working, but it is not applied everywhere. `formatLength()` (client/src/utils/parsing.tsx:154-161) auto-switches an mm value to meters once it reaches 1000mm, and its own unit test asserts `formatLength(1000)` … |
| [#86](https://github.com/sherrmann/Spoolman/issues/86) | feature | P2 | S | [#327](https://github.com/Donkie/Spoolman/issues/327), [#729](https://github.com/Donkie/Spoolman/issues/729) | **Add a vendor/manufacturer filter column to the Spool list and a 'view spools' shortcut from the Vendor page** (high confidence) — The backend already supports filtering spools by vendor: spoolman/api/v1/spool.py:216-227 defines the `filament.vendor.id` query param and spoolman/api/v1/spool.py:203-215 defines `filament.vendor.name`, both wired through to spoolman/database/spool.py:143-144 (`add_where_clause_int_opt(models.Filam… |
| [#88](https://github.com/sherrmann/Spoolman/issues/88) | feature | P2 | L | [#552](https://github.com/Donkie/Spoolman/issues/552), [#740](https://github.com/Donkie/Spoolman/issues/740) | **Support attaching a reference image/photo to a filament** (high confidence) — The fork's Filament ORM model (spoolman/database/models.py:32-63) and its Pydantic API schema (spoolman/api/v1/models.py:103-190) carry no image, photo, or picture column of any kind, and neither the create nor edit filament forms (client/src/pages/filaments/create.tsx, client/src/pages/filaments/ed… |
| [#91](https://github.com/sherrmann/Spoolman/issues/91) | feature | P2 | M | [#559](https://github.com/Donkie/Spoolman/issues/559), [#567](https://github.com/Donkie/Spoolman/issues/567) | **Persist additional SpoolmanDB catalog fields (spool_type, finish, pattern, translucent, glow) on the local Filament** (high confidence) — The fork's own external-catalog schema already carries the fields this issue wants surfaced: ExternalFilament in spoolman/externaldb.py:64-95 defines spool_type (line 72), finish (line 86), pattern (line 92), translucent (line 93) and glow (line 94), sourced from the fork-owned SpoolmanDB catalog (s… |
| [#93](https://github.com/sherrmann/Spoolman/issues/93) | feature | P2 | M | [#380](https://github.com/Donkie/Spoolman/issues/380), [#755](https://github.com/Donkie/Spoolman/issues/755) | **Track label-printed status per spool/filament and use it in the print flow (hide/mark printed, warn before reprint)** (high confidence) — There is no 'label printed' concept anywhere in the fork's spool data or printing flow, so the requested filter cannot be built without new state. The spool model (client/src/pages/spools/model.tsx:9-27) has no printed/label_printed_at field, matching a grep of spoolman/database/models.py which show… |
| [#95](https://github.com/sherrmann/Spoolman/issues/95) | feature | P2 | M | [#779](https://github.com/Donkie/Spoolman/issues/779), [#883](https://github.com/Donkie/Spoolman/issues/883) | **Add a UI to export/print the full inventory list (CSV/JSON export button and a printable report with color swatches)** (high confidence) — The backend already has raw list export endpoints - `GET /export/spools`, `/export/filaments`, `/export/vendors` (spoolman/api/v1/export.py:28-71) dump CSV or JSON via `dump_as_csv`/`dump_as_json` (export.py:74-88) - but a repo-wide search for '/export' in client/src returns no matches, meaning ther… |
| [#97](https://github.com/sherrmann/Spoolman/issues/97) | feature | P2 | M | [#789](https://github.com/Donkie/Spoolman/issues/789), [#887](https://github.com/Donkie/Spoolman/issues/887) | **Let the camera scanner read additional barcode formats (data matrix/UPC/EAN) and support retail-barcode spool lookup/create** (high confidence) — The in-app scanner is hardcoded to a single symbology and a single payload grammar: client/src/components/qrCodeScanner.tsx:32-37 passes `formats={["qr_code"]}` to the `@yudiel/react-qr-scanner` `Scanner` component, so UPC/EAN retail barcodes are never even detected by the camera, and `parseScanResu… |
| [#76](https://github.com/sherrmann/Spoolman/issues/76) | feature | P2 | L | [#11](https://github.com/Donkie/Spoolman/issues/11) | **Export filament profiles in slicer-native formats (PrusaSlicer/OrcaSlicer/Bambu Studio/Cura)** (medium confidence) — The fork's export capability is generic, not slicer-aware: spoolman/api/v1/export.py:23-88 only offers CSV/JSON dumps of Spoolman's own filament/spool/vendor rows via spoolman/export.py's dump_as_csv/dump_as_json, with no slicer-specific profile generator anywhere in the repo. A repo-wide search for… |
| [#77](https://github.com/sherrmann/Spoolman/issues/77) | feature | P2 | S | [#139](https://github.com/Donkie/Spoolman/issues/139) | **Add a one-click 'Reset usage' action for spools** (high confidence) — The backend already supports exactly the request in a single call: `SpoolUpdateParameters` (spoolman/api/v1/spool.py:32-94, via `SpoolParameters`) has `used_weight`, `first_used`, and `last_used` as independently-optional fields, and the PATCH handler `update()` (spoolman/api/v1/spool.py:450-484) do… |
| [#78](https://github.com/sherrmann/Spoolman/issues/78) | feature | P2 | M | [#236](https://github.com/Donkie/Spoolman/issues/236) | **Add SPOOLMAN_DB_SCHEMA env var to select the Postgres/CockroachDB schema** (high confidence) — There is still no way to select a non-default PostgreSQL schema for Spoolman's tables in this fork. A repo-wide search for 'schema' across spoolman/*.py and migrations/ turns up nothing relevant to table placement; spoolman/database/models.py:11-12's `class Base(AsyncAttrs, DeclarativeBase)` never s… |
| [#79](https://github.com/sherrmann/Spoolman/issues/79) | feature | P2 | S | [#249](https://github.com/Donkie/Spoolman/issues/249) | **Add a vertical/rotated text orientation option for QR label printing on narrow labels** (high confidence) — The described layout problem still exists exactly as reported: client/src/pages/printing/qrCodePrintingDialog.tsx:167-201 hard-codes '.print-qrcode-item' as a flex row (display:flex, no flex-direction override) with '.print-qrcode-container' capped at max-width:50% (line 176) and '.print-qrcode-titl… |
| [#80](https://github.com/sherrmann/Spoolman/issues/80) | feature | P2 | S | [#260](https://github.com/Donkie/Spoolman/issues/260) | **Publish a reference client/docs for SPI-GPIO RFID readers (MFRC522/RC-522) against the NFC API** (medium confidence) — The fork's NFC subsystem is architecturally close to what the reporter proposed (a lightweight reader-side client that talks to a remote Spoolman instance), even though it doesn't drive an RC-522 directly. `NfcService` (spoolman/nfc_service.py:37-89) only talks to readers nfcpy recognizes over USB/U… |
| [#81](https://github.com/sherrmann/Spoolman/issues/81) | feature | P2 | L | [#262](https://github.com/Donkie/Spoolman/issues/262) | **Add historical filament consumption and cost statistics (weekly/monthly/annual)** (high confidence) — No time-series usage or cost data is tracked anywhere in the fork's code, so a weekly/monthly/annual consumption-and-cost view as requested cannot currently be built from existing data. The home dashboard's analytics module (client/src/pages/home/analytics.ts, 118 lines total) only computes present-… |
| [#82](https://github.com/sherrmann/Spoolman/issues/82) | feature | P2 | S | [#306](https://github.com/Donkie/Spoolman/issues/306) | **Warn on (or block) creating a vendor whose name already exists** (high confidence) — The fork still has no duplicate-name protection anywhere in the vendor-creation path, so the exact scenario in the report (accidentally entering the same vendor twice) still reproduces today. `spoolman/database/vendor.py:21-42`'s `create()` unconditionally inserts a new `models.Vendor` row with what… |
| [#83](https://github.com/sherrmann/Spoolman/issues/83) | feature | P2 | M | [#334](https://github.com/Donkie/Spoolman/issues/334) | **Surface filament-level custom fields on the Spool list/detail views** (medium confidence) — The Spool list and show pages only fetch and render the SPOOL entity's own extra-field definitions: client/src/pages/spools/list.tsx:104 calls `useGetFields(EntityType.spool)` and builds columns solely from that (client/src/pages/spools/list.tsx:454-459), and client/src/pages/spools/show.tsx:35,258 … |
| [#87](https://github.com/sherrmann/Spoolman/issues/87) | feature | P2 | M | [#396](https://github.com/Donkie/Spoolman/issues/396) | **Localize date/time display format to the active UI language (or add a date-format setting)** (high confidence) — Every date/time display and picker in the client hardcodes an ISO-style format string instead of a locale-aware one, so switching the UI language never changes how dates are ordered (e.g. to Germany's DD.MM.YYYY). |
| [#89](https://github.com/sherrmann/Spoolman/issues/89) | feature | P2 | M | [#406](https://github.com/Donkie/Spoolman/issues/406) | **Publish a Home Assistant Supervisor add-on packaging for Spoolman** (medium confidence) — There is no Home Assistant Supervisor add-on packaging anywhere in this repository: a search for hassio/homeassistant/addon config files (the manifest format Home Assistant requires, e.g. config.yaml/config.json with io.hass.* labels) returns nothing, only a standard multi-stage Dockerfile (Dockerfi… |
| [#90](https://github.com/sherrmann/Spoolman/issues/90) | feature | P2 | M | [#430](https://github.com/Donkie/Spoolman/issues/430) | **Add resizable column widths to Spool/Filament list tables** (medium confidence) — This report bundles four asks that land differently against current code. First, ad-hoc project groups of spools: there is no saved/named view concept, since client/src/utils/saveload.ts:8-45 persists only a single current filter/sort/column-visibility state per table (shareable via URL hash), so a … |
| [#92](https://github.com/sherrmann/Spoolman/issues/92) | feature | P2 | S | [#514](https://github.com/Donkie/Spoolman/issues/514) | **Add a settings-driven list of custom external links in the sidebar** (medium confidence) — The sidebar is built entirely from Refine's registered-resource menu items plus two hardcoded extra entries ('/settings', '/help') sorted to the bottom (client/src/components/layout.tsx:9-48, bottomKeys at line 18); there is no mechanism anywhere in client/src or spoolman/api for a user to add their… |
| [#94](https://github.com/sherrmann/Spoolman/issues/94) | feature | P2 | M | [#520](https://github.com/Donkie/Spoolman/issues/520) | **Add drag-to-reorder table columns and split the Spool list's Filament column into Vendor + Name** (high confidence) — The Spools list still combines vendor and filament name into one string in `collapseSpool()` (client/src/pages/spools/list.tsx:53-69, `filament.combined_name`) and renders it as a single `SpoolIconColumn` (client/src/pages/spools/list.tsx:356-369), so there is no separate Manufacturer column on the … |
| [#96](https://github.com/sherrmann/Spoolman/issues/96) | feature | P2 | S | [#546](https://github.com/Donkie/Spoolman/issues/546) | **Document Podman / rootless + SELinux setup in the installation guide** (medium confidence) — Upstream report (7 comments): running the compose file under rootless Podman on Fedora crash-loops - `Failed to setup disk-based cache due to permission error`, then `Data directory is not writable`, with `chown: cannot read directory '/home/app/.local/share/spoolman': Permission denied`, ending in … |
| [#98](https://github.com/sherrmann/Spoolman/issues/98) | feature | P2 | S | [#558](https://github.com/Donkie/Spoolman/issues/558) | **Log spool use/measure API mutations (id, requested value, resulting delta) for debugging** (high confidence) — Spoolman's logging is still limited to process-level startup/shutdown messages and uvicorn's generic access log (spoolman/main.py:22-44), which at most records method/path/status per request and never the request body, so a log line for `PUT /api/v1/spool/5/use` shows nothing about the actual `use_w… |
| [#99](https://github.com/sherrmann/Spoolman/issues/99) | feature | P2 | M | [#571](https://github.com/Donkie/Spoolman/issues/571) | **Add a header-level bulk 'Update Spool Weight' workflow with search/filter and save-and-update-more** (high confidence) — The spool list header today only offers a Print-QR button, an archived-spools toggle, a clear-filters button, and a column-visibility dropdown (client/src/pages/spools/list.tsx:260-291) - there is no header-level 'update spool weight' entry point. Weight adjustment is still only available per-row: t… |
| [#100](https://github.com/sherrmann/Spoolman/issues/100) | feature | P2 | S | [#583](https://github.com/Donkie/Spoolman/issues/583) | **Show a quick list of sibling spools (same filament) on the Spool Show page** (high confidence) — The Spool Show page (rendered after a QR scan navigates to /spool/show/{id}) has no notion of sibling spools sharing the same filament: client/src/pages/spools/show.tsx lists every spool field in sequence and ends at line 264 with the extra-fields block, with no query for or rendering of other spool… |
| [#102](https://github.com/sherrmann/Spoolman/issues/102) | feature | P2 | M | [#606](https://github.com/Donkie/Spoolman/issues/606) | **Add a ZPL (Zebra thermal printer) export option for spool/filament labels** (high confidence) — Label output is still limited to on-screen/browser printing and PNG export: client/src/pages/printing/printingDialog.tsx:187-217 defines saveAsImage(), which rasterizes each label node with html-to-image's toPng() and triggers a PNG download, and the only two action buttons offered are 'Save as Imag… |
| [#104](https://github.com/sherrmann/Spoolman/issues/104) | feature | P2 | L | [#609](https://github.com/Donkie/Spoolman/issues/609) | **Add spool weight-history logging with moisture-drift estimation** (medium confidence) — There is still no way in the fork to track a spool's gross weight over time or estimate moisture, so the request stands. The only weight-input mechanism is POST /spool/{id}/measure (spoolman/api/v1/spool.py:540-560), which calls spoolman/database/spool.py:390-455's measure(); that function only read… |
| [#105](https://github.com/sherrmann/Spoolman/issues/105) | feature | P2 | L | [#614](https://github.com/Donkie/Spoolman/issues/614) | **Import a sliced .3mf project to auto-select and bulk-adjust matching spools** (medium confidence) — There is no code path that reads an uploaded/sliced .3mf project to extract per-plate filament colors, materials, or usage. A repo-wide search for '3mf' only turns up the fork's own outbound swatch generator, client/src/utils/swatch/threeMf.ts:1-18, which serializes a 3D-printable color-swatch keych… |
| [#107](https://github.com/sherrmann/Spoolman/issues/107) | feature | P2 | S | [#656](https://github.com/Donkie/Spoolman/issues/656) | **Add QR-code placement (side/above/below) and per-line text sizing to label print templates** (medium confidence) — This is a bundle of four first-time-user suggestions, and the fork's code already resolves most of them but leaves one concrete gap worth adopting. A curated material-type dropdown with editable per-type defaults does not exist: the material field in client/src/pages/filaments/create.tsx:256-267 is … |
| [#110](https://github.com/sherrmann/Spoolman/issues/110) | feature | P2 | S | [#693](https://github.com/Donkie/Spoolman/issues/693) | **Add a camera device selector to the QR scanner modal** (high confidence) — The app-wide QR scanner modal at client/src/components/qrCodeScanner.tsx:9-76 wraps the `Scanner` component from `@yudiel/react-qr-scanner` (client/package.json:19, pinned to ^2.5.0) and only ever passes `constraints={{ facingMode: "environment" }}` (qrCodeScanner.tsx:33-35); there is no UI to enume… |
| [#112](https://github.com/sherrmann/Spoolman/issues/112) | feature | P2 | M | [#703](https://github.com/Donkie/Spoolman/issues/703) | **Support extruder/bed temperature ranges (not just single values) on filaments** (high confidence) — The Filament ORM model only stores single-value overrides, settings_extruder_temp and settings_bed_temp (spoolman/database/models.py:49-50), mirrored 1:1 in the API schema (spoolman/api/v1/models.py:149-160) with no min/max or range columns anywhere. Tellingly, the fork's own 3D Filament Profiles im… |
| [#114](https://github.com/sherrmann/Spoolman/issues/114) | feature | P2 | S | [#713](https://github.com/Donkie/Spoolman/issues/713) | **Add an optional colour swatch to printed spool/filament labels** (high confidence) — The request asks to optionally include a rendered colour swatch (an actual coloured box, not just text) on printed spool/filament labels, mirroring the colour chip already shown on the spool/filament detail pages. The label content pipeline in client/src/pages/printing/qrCodePrintingDialog.tsx:51-73… |
| [#101](https://github.com/sherrmann/Spoolman/issues/101) | feature | P2 | M | [#720](https://github.com/Donkie/Spoolman/issues/720) | **Add a per-spool filament diameter override** (high confidence) — Filament diameter is modeled exclusively at the Filament level, with no per-spool override anywhere in the schema or calculations. spoolman/database/models.py:44 defines Filament.diameter as a required (non-nullable) column, while the Spool class (models.py:66-87) has no diameter field at all. |
| [#103](https://github.com/sherrmann/Spoolman/issues/103) | feature | P2 | L | [#763](https://github.com/Donkie/Spoolman/issues/763) | **Promote locations to a first-class entity with support for custom/extra fields** (high confidence) — Locations are not a first-class entity anywhere in the fork: there is no Location table in spoolman/database/models.py (only Vendor, Filament, Spool and their *Field side-tables exist, plus Calibration/Setting), and the client instead treats a location purely as a string stored inside two JSON blobs… |
| [#106](https://github.com/sherrmann/Spoolman/issues/106) | feature | P2 | S | [#769](https://github.com/Donkie/Spoolman/issues/769) | **Expose QR error-correction level (module density) as a user setting** (high confidence) — The QR error-correction level is hardcoded rather than configurable, which is the main lever affecting module count: `errorLevel: "H"` is fixed in both client/src/pages/printing/spoolQrCodePrintingDialog.tsx:323 and filamentQrCodePrintingDialog.tsx:292, and the shared dialog that renders the actual … |
| [#108](https://github.com/sherrmann/Spoolman/issues/108) | feature | P2 | S | [#807](https://github.com/Donkie/Spoolman/issues/807) | **Add query-parameter filtering to GET /api/v1/external/filament** (high confidence) — `GET /api/v1/external/filament` (spoolman/api/v1/externaldb.py:62-76) takes no query parameters at all: it unconditionally loads every cached filament from spoolmandb, plus TigerTag entries when enabled (the `_load_filament_source` calls at lines 69-71), validates each one, and returns the full merg… |
| [#109](https://github.com/sherrmann/Spoolman/issues/109) | feature | P2 | M | [#834](https://github.com/Donkie/Spoolman/issues/834) | **Add per-filament low-stock threshold with aggregate remaining-weight alert** (high confidence) — Upstream wants a per-filament minimum-quantity field plus a UI alert/indicator when the aggregate remaining weight across all of that filament's spools drops below it, and describes hand-writing a MySQL query against a filament extra field as their current workaround. The fork's only built-in low-st… |
| [#111](https://github.com/sherrmann/Spoolman/issues/111) | feature | P2 | M | [#847](https://github.com/Donkie/Spoolman/issues/847) | **Build the client bundle inside the Dockerfile (native-arch build stage)** (high confidence) — The root Dockerfile still requires a pre-built client bundle to already exist on the host before `docker build .` is invoked: `COPY --chown=app:app ./client/dist /home/app/spoolman/client/dist` (Dockerfile:78) has no preceding Node/Vite build stage, so a fresh clone with no `client/dist` directory f… |
| [#113](https://github.com/sherrmann/Spoolman/issues/113) | feature | P2 | M | [#853](https://github.com/Donkie/Spoolman/issues/853) | **Add a sort-by-hue option to the spool/filament list views** (medium confidence) — Upstream wants to sort the spool list by color hue (reds together, then yellows, then greens) to match how they physically organize spools on a shelf, and included screenshots showing that sorting by filament name is alphabetical and sorting by location is effectively unordered within a location. Th… |
| [#115](https://github.com/sherrmann/Spoolman/issues/115) | feature | P2 | S | [#870](https://github.com/Donkie/Spoolman/issues/870) | **Add empty spool weight as an optional spool-list column** (high confidence) — Upstream wants the existing per-spool empty/tare weight field surfaced as an optional column in the spools list view, since today the only way to check whether it was filled in is to open each spool's edit page. The spool list's column chooser is driven by a fixed allColumns array (client/src/pages/… |
| [#116](https://github.com/sherrmann/Spoolman/issues/116) | feature | P2 | L | [#882](https://github.com/Donkie/Spoolman/issues/882) | **Add reserve/unopened-stock counts per filament with a low-stock shopping list (without one Spool row per unit)** (medium confidence) — The fork has no concept of a spare/reserve stock count decoupled from a full Spool row: spoolman/database/models.py:66-88 shows Spool has only initial_weight, spool_weight, used_weight, location, lot_nr, comment, archived, and extra fields, with no quantity/spare_count column, and Filament (models.p… |
| [#117](https://github.com/sherrmann/Spoolman/issues/117) | feature | P2 | S | [#934](https://github.com/Donkie/Spoolman/issues/934) | **Persist the last-used spool consumption mode (length/weight/measured weight) across sessions** (high confidence) — The 'use spool' modal's mode selector is not persisted: client/src/pages/spools/functions.tsx:204 initializes measurementType with a plain useState<MeasurementType>('length'), hard-coding 'length' as the default on every mount, and the radio group at functions.tsx:244-253 only updates that in-memory… |
| [#118](https://github.com/sherrmann/Spoolman/issues/118) | feature | P2 | M | [#937](https://github.com/Donkie/Spoolman/issues/937) | **Support linked custom-field definitions with copy-on-create or live sync between Filament and Spool** (medium confidence) — Extra/custom fields are strictly scoped per entity type with no cross-entity sharing or value inheritance. spoolman/settings.py:73-75 registers extra_fields_vendor, extra_fields_filament, and extra_fields_spool as three independent settings keys with no shared namespace. |
| [#119](https://github.com/sherrmann/Spoolman/issues/119) | feature | P2 | S | [#947](https://github.com/Donkie/Spoolman/issues/947) | **Add a system/auto theme mode that live-follows the OS color-scheme preference** (high confidence) — The requested third 'follow system' theme option does not exist. client/src/contexts/color-mode/index.tsx:12-16 reads `window.matchMedia("(prefers-color-scheme: dark)")` only once, at mount, purely to seed the initial `mode` state (`colorModeFromLocalStorage \|\| systemPreference`), and lines 18-20 im… |
| [#120](https://github.com/sherrmann/Spoolman/issues/120) | fix | P3 | S | [#44](https://github.com/Donkie/Spoolman/issues/44) | **Migrate spool/filament/vendor show pages from stacked Title+Field to antd Descriptions** (high confidence) — All three primary entity 'show' pages still render fields as a manually repeated `<Typography.Title level={5}>{label}</Typography.Title>` immediately followed by a separate Field component, exactly the pattern this issue (filed by the original maintainer) asked to replace with antd's Descriptions co… |
| [#121](https://github.com/sherrmann/Spoolman/issues/121) | fix | P3 | S | [#597](https://github.com/Donkie/Spoolman/issues/597) | **Fix low-contrast swatch border for dark/black filaments in dark mode** (high confidence) — The color-swatch square/circle rendered for each filament (client/src/components/spoolIcon.tsx:9-33) is styled by client/src/components/spoolIcon.css:22-26, which hardcodes a single border of `#44444430` (dark grey at roughly 19% opacity) on every swatch segment regardless of theme. The app's dark-m… |
| [#122](https://github.com/sherrmann/Spoolman/issues/122) | fix | P3 | S | [#771](https://github.com/Donkie/Spoolman/issues/771) | **Give spool-table date columns explicit widths so they stop ballooning when empty** (medium confidence) — The spools table renders with tableLayout="auto" and scroll={{ x: "max-content" }} (client/src/pages/spools/list.tsx:332-333), but the date columns first_used, last_used and registered are built via DateColumn with no explicit width (client/src/pages/spools/list.tsx:439-453), and DateColumn itself s… |
| [#124](https://github.com/sherrmann/Spoolman/issues/124) | fix | P3 | S | [#774](https://github.com/Donkie/Spoolman/issues/774) | **Location page: fall back to spool.initial_weight for total weight when the filament has no weight** (medium confidence) — The report claims a two-color Bambu filament shows no remaining/total weight on the Location page, but the Location card's subtitle builder does not branch on color at all: formatSubtitle only emits weight text when spool.filament.weight is truthy and always uses spool.filament.weight as the total (… |
| [#123](https://github.com/sherrmann/Spoolman/issues/123) | feature | P3 | S | [#303](https://github.com/Donkie/Spoolman/issues/303), [#336](https://github.com/Donkie/Spoolman/issues/336) | **Document full uninstall (native/systemd and Docker) and migrating an install between native and Docker** (high confidence) — The native Linux installer (scripts/install.sh) leaves several persistent artifacts on the host with no corresponding removal path: it can install a system-wide systemd unit at /etc/systemd/system/Spoolman.service that is enabled and started (scripts/install.sh:206-215), it creates a Python virtual … |
| [#125](https://github.com/sherrmann/Spoolman/issues/125) | feature | P3 | S | [#65](https://github.com/Donkie/Spoolman/issues/65) | **Allow creating a new vendor inline from the filament create form** (high confidence) — The filament create form's vendor picker (client/src/pages/filaments/create.tsx:178-199) is a plain Refine `useSelect` Select with `allowClear` (create.tsx:65-69) and no way to add a new vendor without leaving the page; the only way today is to abandon the in-progress filament form, go to /vendor/cr… |
| [#126](https://github.com/sherrmann/Spoolman/issues/126) | feature | P3 | S | [#191](https://github.com/Donkie/Spoolman/issues/191) | **Show filament color in the spool-create filament dropdown** (medium confidence) — The Spool Create filament dropdown (client/src/pages/spools/create.tsx:316-321) is populated via `formatFilamentLabel` (client/src/pages/spools/functions.tsx:91-116), which today already packs in vendor, name, material, diameter, weight, and spool type - substantially more than the bare 'name + mate… |
| [#127](https://github.com/sherrmann/Spoolman/issues/127) | feature | P3 | S | [#202](https://github.com/Donkie/Spoolman/issues/202) | **Auto-focus the first field and submit on Enter in entity Create forms** (high confidence) — The issue's own follow-up request, a “Save and Create another” button, is already implemented: the Spool, Filament and Vendor Create pages all render a `t("buttons.saveAndAdd")` button next to Save that calls `handleSubmit("create")` (client/src/pages/spools/create.tsx:272-274, client/src/pages/fila… |
| [#128](https://github.com/sherrmann/Spoolman/issues/128) | feature | P3 | S | [#274](https://github.com/Donkie/Spoolman/issues/274) | **Make the Save / Save & Add button bar sticky on long Create forms** (high confidence) — On every Create page (Filament, Spool, Vendor) the Save/Save-and-Add buttons are only rendered through Refine's footerButtons render prop, which places them in normal document flow at the very bottom of the form: client/src/pages/filaments/create.tsx:130-139, client/src/pages/spools/create.tsx:256-2… |
| [#129](https://github.com/sherrmann/Spoolman/issues/129) | feature | P3 | M | [#308](https://github.com/Donkie/Spoolman/issues/308) | **Add a 'link' custom field type with base-URL templating** (high confidence) — The fork's extra-field system enumerates exactly eight field types text/integer/integer_range/float/float_range/datetime/boolean/choice (spoolman/extra_field_registry.py:32-40, mirrored in client/src/utils/queryFields.ts:5-14) with no link/URL type, and the display/form components in client/src/comp… |
| [#130](https://github.com/sherrmann/Spoolman/issues/130) | feature | P3 | M | [#329](https://github.com/Donkie/Spoolman/issues/329) | **Emit spool websocket events when a spool's linked filament or vendor changes** (medium confidence) — The websocket system is strictly per-entity: filament_changed() only broadcasts on the ('filament', id) pool (spoolman/database/filament.py:323-327) and is the only notification fired from filament.update() (spoolman/database/filament.py:243); vendor_changed() likewise only broadcasts on ('vendor', … |
| [#131](https://github.com/sherrmann/Spoolman/issues/131) | feature | P3 | S | [#374](https://github.com/Donkie/Spoolman/issues/374) | **Add a compact all-spools color-swatch grid to the Home dashboard** (high confidence) — The redesigned home dashboard has no view that shows every spool as a compact color swatch. It only surfaces a Low Stock tab listing spools below the low-stock threshold with one small color dot each (client/src/pages/home/index.tsx:262-309), aggregate bar charts for material/vendor/location breakdo… |
| [#132](https://github.com/sherrmann/Spoolman/issues/132) | feature | P3 | S | [#418](https://github.com/Donkie/Spoolman/issues/418) | **Reserve and document a WEB+SPOOLMAN 'clear spool' QR/NFC sentinel value** (medium confidence) — No such convention exists today. The WEB+SPOOLMAN scheme only recognises spool and filament codes: PREFIX = { spool: 'S', filament: 'F' } and the parser regexes only match 's-<id>'/'f-<id>' (client/src/utils/scan.ts:15,30-33); any other payload, including a would-be 'clear' value, returns null from … |
| [#133](https://github.com/sherrmann/Spoolman/issues/133) | feature | P3 | S | [#467](https://github.com/Donkie/Spoolman/issues/467) | **Add an optional multi-column CSS layout for label text content** (medium confidence) — Each printed label lays out an optional QR code on the left and a single flexible text block on the right that just word-wraps and clips: `.print-qrcode-title { flex: 1 1 auto; ...; overflow: hidden; }` (client/src/pages/printing/qrCodePrintingDialog.tsx:186-191), inside a `.print-qrcode-item { disp… |
| [#134](https://github.com/sherrmann/Spoolman/issues/134) | feature | P3 | S | [#505](https://github.com/Donkie/Spoolman/issues/505) | **Add a filter-aware totals/summary row to the Spool (and Filament) list tables** (high confidence) — The Spool list table still has no aggregate/summary row. client/src/pages/spools/list.tsx:329-334 renders a plain antd `Table` (`sticky`, `tableLayout="auto"`, `dataSource={dataSource}`) with no `summary` render prop, and a repo-wide search for `Table.Summary`/`summary=` across client/src returns ze… |
| [#136](https://github.com/sherrmann/Spoolman/issues/136) | feature | P3 | S | [#545](https://github.com/Donkie/Spoolman/issues/545) | **Document that printers are Spoolman clients (moonraker.conf [spoolman] example distinct from update-manager)** (medium confidence) — The current docs still describe how to install the Spoolman SERVER (Docker or native) but never spell out that a printer/Moonraker host is a CLIENT that just needs a config stanza pointing at that server. docs/installation.md:56-69 and README.md:102-114 both show a moonraker.conf snippet, but it is … |
| [#138](https://github.com/sherrmann/Spoolman/issues/138) | feature | P3 | M | [#664](https://github.com/Donkie/Spoolman/issues/664) | **Add an optional 1D barcode alongside the QR code on printed labels** (high confidence) — Label generation only supports 2D QR codes, not 1D barcodes. client/src/pages/printing/qrCodePrintingDialog.tsx:51-73 builds each printable label block from a single antd QRCode component (type='svg'); there is no alternate/additional code-symbology renderer anywhere in that dialog or in printingDia… |
| [#139](https://github.com/sherrmann/Spoolman/issues/139) | feature | P3 | M | [#768](https://github.com/Donkie/Spoolman/issues/768) | **Add an optional grid/gallery view mode to the Spool list** (medium confidence) — The primary Spool list is table-only: client/src/pages/spools/list.tsx:329-468 renders a single antd `<Table>` with no alternate view mode or toggle, and there is no "gallery", "grid", or "card view" concept anywhere under client/src/pages/spools/. A related but distinct card component already exist… |
| [#140](https://github.com/sherrmann/Spoolman/issues/140) | feature | P3 | M | [#783](https://github.com/Donkie/Spoolman/issues/783) | **Add configurable custom action links/buttons per spool** (high confidence) — The spool show page only renders a fixed, hardcoded set of buttons: Adjust, Use Filament, NFC Bind, NFC Write, and Archive (client/src/pages/spools/show.tsx:135-173), and the list view's per-row action menu is likewise a fixed array built in `actions` (client/src/pages/spools/list.tsx:213-230) feedi… |
| [#141](https://github.com/sherrmann/Spoolman/issues/141) | feature | P3 | L | [#841](https://github.com/Donkie/Spoolman/issues/841) | **Support vendor/filament logo images and standard small-label size presets in label printing** (medium confidence) — There is no logo/brand-image support anywhere in the label-printing code: `renderLabelContents`/`getTagValue` (client/src/pages/printing/printing.tsx:63-128) only substitute text tags into a template string and have no image/icon placeholder syntax, and a repo-wide search for 'logo' turns up only un… |
| [#135](https://github.com/sherrmann/Spoolman/issues/135) | feature | P3 | S | [#843](https://github.com/Donkie/Spoolman/issues/843) | **Add a 'transfer weight to another spool' option to the spool adjust modal** (medium confidence) — The spool 'adjust' modal only ever targets one spool at a time: `useSpoolAdjustModal` (client/src/pages/spools/functions.tsx:199-272) takes a single `curSpool`, lets the user pick length/weight/measured-weight, and submits via `useSpoolFilament`/`useSpoolFilamentMeasure` (functions.tsx:33-65), which… |
| [#137](https://github.com/sherrmann/Spoolman/issues/137) | feature | P3 | S | [#884](https://github.com/Donkie/Spoolman/issues/884) | **Add a custom/raw QR payload option to spool & filament label printing** (medium confidence) — The spool and filament label QR printers always encode one of two fixed payload formats and give the user no way to substitute arbitrary text: client/src/pages/printing/spoolQrCodePrintingDialog.tsx:311 sets `value: useHTTPUrl ? \`${baseUrlRoot}/spool/show/${spool.id}\` : \`WEB+SPOOLMAN:S-${spool.id… |

## Skipped — kept out of the tracker deliberately

| Upstream | Reason | Title / why |
|---|---|---|
| [#18](https://github.com/Donkie/Spoolman/issues/18) | already-implemented | **Mobile Support** — The fork ships a full installable PWA with mobile icon assets and manifest config (client/vite.config.ts:16-40 `VitePWA({ manifest: {...} })`, plus client/public/pwa-192x192.png, pwa-512x512.png, maskable-icon-512x512.png, apple-touch-icon-180x180.png) and a proper responsive viewport meta tag (clie… |
| [#33](https://github.com/Donkie/Spoolman/issues/33) | already-implemented | **Database of Spool Weights by Manufacturer** — The fork already syncs a central, crowdsourced external filament catalog and surfaces manufacturer empty-spool weights from it. `ExternalFilament` (spoolman/externaldb.py:64-95) carries both `manufacturer` (line 66) and `spool_weight` ("Weight of an empty spool", line 71), synced from `DEFAULT_EXTER… |
| [#42](https://github.com/Donkie/Spoolman/issues/42) | already-implemented | **Add possibility to add/edit spool length** — Spool length is tracked, displayed, and directly editable throughout the app today, not weight-only. `Spool.from_db()` computes `used_length` and `remaining_length` from `used_weight`/`initial_weight` via `length_from_weight()` (spoolman/api/v1/models.py:273-286, 313-341; spoolman/math.py:25), and b… |
| [#46](https://github.com/Donkie/Spoolman/issues/46) | already-implemented | **Add create w/spool to filament creation page** — The literal ask — a button on the Filament creation page to also create a spool — doesn't exist: the footer of client/src/pages/filaments/create.tsx (lines 130-139) only offers `buttons.save` and `buttons.saveAndAdd` (save-and-add-another-filament), with no spool-creation shortcut. However, the fork… |
| [#49](https://github.com/Donkie/Spoolman/issues/49) | already-fixed | **Filaments: Name in Grid is not updated after change** — The reported defect (a filament's name in the list grid not refreshing after an edit) is resolved in the fork's current client architecture. The filament list subscribes each visible row to live websocket updates via useLiveify('filament', queryDataSource, collapseFilament) at client/src/pages/filam… |
| [#66](https://github.com/Donkie/Spoolman/issues/66) | superseded | **QR Code scanning add button with a supported data for a db entry embedded in the QR code.** — The QR scanner (client/src/components/qrCodeScanner.tsx:15-24) only recognizes Spoolman's own two payload formats via parseScanResult (client/src/utils/scan.ts:30-51): the custom WEB+SPOOLMAN:S-<id>/F-<id> scheme and Spoolman's own deep-link URLs. Any other QR content is silently ignored (the onScan… |
| [#68](https://github.com/Donkie/Spoolman/issues/68) | already-implemented | **Allow the QR code to be configured as a URL** — Spoolman's QR payload builder already supports encoding a plain HTTP(S) URL instead of the custom scheme: buildScanPayload(resource, id, baseUrl) returns `${baseUrl}/${resource}/show/${id}` whenever a baseUrl is supplied, and only falls back to WEB+SPOOLMAN:S-<id>/F-<id> when it is not (client/src/u… |
| [#72](https://github.com/Donkie/Spoolman/issues/72) | out-of-scope | **Plugin system for users to add features they think are a good idea but may not directly coincide with the projects overall goals.** — No plugin or extension system exists anywhere in the fork: there is no dynamic module loading, entry-point registry, or hook mechanism in the backend or client, and the only backend hit for the word 'plugin' is an unrelated comment about the vite-plugin-pwa build tool (spoolman/client.py:67). The is… |
| [#74](https://github.com/Donkie/Spoolman/issues/74) | already-fixed | **Improve label layout** — The reported failure - printing a single 54x17mm label on a Brother printer produces two extra blank labels - stems from the label sheet being locked to fixed paper presets that don't match small continuous-roll label stock. The current sheet designer removes that constraint: it supports a fully cus… |
| [#76](https://github.com/Donkie/Spoolman/issues/76) | already-implemented | **Show all filament spools in the filament page** — Both halves of this request already exist in the fork. On the Spool Show page, the Filament field renders as a clickable link to the corresponding filament: `filamentURL` builds `<a href="/filament/show/{id}">` (client/src/pages/spools/show.tsx:111-114) and is used directly under the 'Filament' titl… |
| [#101](https://github.com/Donkie/Spoolman/issues/101) | already-implemented | **Set default Value in Create Filament and "Save and Continue" Option** — The concrete, actionable half of this request, a 'save and continue' action that submits the current create form and immediately clears it for the next entry, is already implemented on every relevant create form: the filament create form has both 'Save' and 'Save and Add' footer buttons calling hand… |
| [#126](https://github.com/Donkie/Spoolman/issues/126) | already-fixed | **Hamburger Menu oddly positioned on mobile (webapp)** — The reporter asked for the mobile hamburger/sidebar-toggle button to live in the header bar instead of floating oddly over the page content on iOS Safari's installed webapp. The fork has already implemented exactly this: client/src/components/layout.css:43-53 contains a mobile-only override explicit… |
| [#129](https://github.com/Donkie/Spoolman/issues/129) | out-of-scope | **Swap/add vendor and manufacturer properties** — The fork still models exactly what upstream did: `Filament.vendor_id`/`vendor` is a foreign key to `Vendor` (spoolman/database/models.py:32-38), `Spool` has no vendor/reseller field at all - only filament, price, location, lot_nr, comment, archived, extra (spoolman/database/models.py:66-71) - and th… |
| [#142](https://github.com/Donkie/Spoolman/issues/142) | already-implemented | **Centralized database of Vendors and Filaments** — This feature is fully implemented in the fork. spoolman/externaldb.py defines an ExternalFilament/ExternalFilamentsFile model synced from a configurable community catalog, defaulting to DEFAULT_EXTERNAL_DB_URL = 'https://sherrmann.github.io/SpoolmanDB/' (spoolman/externaldb.py:22), refreshed on an i… |
| [#144](https://github.com/Donkie/Spoolman/issues/144) | already-implemented | **Non-uniform colors** — The Filament model already stores multi-color data separately from the single color_hex: multi_color_hexes (comma separated hex list, up to 128 chars) and multi_color_direction ('coaxial' or 'longitudinal') at spoolman/database/models.py:51-53. The filament create/edit forms expose a single-vs-multi… |
| [#178](https://github.com/Donkie/Spoolman/issues/178) | already-implemented | **Right alignment of numbers** — The reporter asked that numeric columns in list views be right-aligned, like a spreadsheet, for readability. This is already implemented fork-wide: client/src/components/column.tsx:375-396 defines a shared `NumberColumn` helper that always sets `align: "right"` (column.tsx:378) and is used for every… |
| [#179](https://github.com/Donkie/Spoolman/issues/179) | already-fixed | **List caching are not refreshed** — The reported defect (a newly added spool/filament not showing in the list, even after hard refresh, with the API returning 304) is addressed in the fork's current code. All three list pages now force a react-query refetch on add/delete: the spool list handles created/deleted events with invalidate({… |
| [#200](https://github.com/Donkie/Spoolman/issues/200) | already-implemented | **Add Transmission Distance to use in HueForge** — The fork's Filament model (spoolman/database/models.py:32-59) has no built-in transmission-distance or light-transmission column, and no HueForge-specific export exists anywhere in the codebase (a repo-wide search for 'hueforge' returns zero matches). However, the fork's generic extra-fields system … |
| [#203](https://github.com/Donkie/Spoolman/issues/203) | already-implemented | **User Presets for pre-populated values** — The fork has no dedicated 'default values for standard fields' settings screen -- every client-side useGetSetting("...") call resolves to just base_url, currency, locations, locations_spoolorders, round_prices and swatch_style as global setting keys, none of which prefill fields like material or ven… |
| [#205](https://github.com/Donkie/Spoolman/issues/205) | superseded (by upstream #212's generic CSV/JSON import re… | **Import CSV from OctoPrint plugin Spools** — Spoolman NG has no data-import capability at all today: spoolman/api/v1/export.py:28-71 only defines GET endpoints to export spools/filaments/vendors as CSV/JSON, spoolman/export.py:41-58 (dump_as_csv) is a one-way dict-flattening dumper with no matching CSV-to-model loader, and there is no file-upl… |
| [#207](https://github.com/Donkie/Spoolman/issues/207) | superseded (by issue 265's filament-grouping/aggregate-st… | **Inventory Management** — The fork already has an archived boolean on Spool (spoolman/database/models.py:82) that GET /api/v1/spool excludes by default unless allow_archived=true is passed (spoolman/api/v1/spool.py:251-253; spoolman/database/spool.py:150-155), which is the same mechanism the issue's proposed second 'storage'… |
| [#226](https://github.com/Donkie/Spoolman/issues/226) | insufficient-info | **Used filament gets subtracted incorrectly - more** — The report is a bare 'me too' (spool shown empty while roughly half remains) with a single screenshot, no version, no reproduction steps, and no indication of whether usage was recorded manually or via the Moonraker/Klipper integration, so no fork code defect can be localized from it. Judged against… |
| [#245](https://github.com/Donkie/Spoolman/issues/245) | already-implemented | **Materials database & button to create spool into filaments creation** — The fork already ships a filament-profile database mechanism that covers the spirit of this request: spoolman/externaldb.py defines ExternalFilament (externaldb.py:63) and ExternalMaterial (externaldb.py:109) models synced from https://sherrmann.github.io/SpoolmanDB/, exposed via /external/filament,… |
| [#246](https://github.com/Donkie/Spoolman/issues/246) | support-question | **Select Spool Not Work In Mainsail** — The report ('select spool does not work in Mainsail, tried many browsers', one screenshot, no logs, no config, no error text) is a deployment/integration support question rather than an identifiable defect in the fork's code. Mainsail's spool selection does not write any Spoolman-side state: it call… |
| [#247](https://github.com/Donkie/Spoolman/issues/247) | already-fixed | **Update from KlipperScreen doesn't restart Spoolman service** — The reported root cause was that after a KlipperScreen/Moonraker-triggered update the install/start scripts were left non-executable, so the venv was deleted but never recreated and the systemd service could not start; the reporter fixed it by chmod +x on install_debian.sh and start.sh. In the fork … |
| [#254](https://github.com/Donkie/Spoolman/issues/254) | already-implemented | **Editing storage location/storage location** — The fork fully implements this request. spoolman/database/spool.py:502-511 defines rename_location(db, current_name, new_name), which runs a single UPDATE spool SET location = new_name WHERE location = current_name so every spool at that location is moved atomically in one statement. |
| [#258](https://github.com/Donkie/Spoolman/issues/258) | already-implemented | **New tab file for active or used coils** — The fork already surfaces an at-a-glance 'active/recently used spools' view without needing a dedicated new tab. The home dashboard's 'Recently Used' panel (client/src/pages/home/index.tsx:400-437) lists the most-recently-used spools sorted by `last_used`, each showing material, remaining weight and… |
| [#263](https://github.com/Donkie/Spoolman/issues/263) | already-fixed | **Display error in spool overview and filament overview** — The report describes table columns overlapping each other in the spool and filament overview lists (a German report with two screenshots, no version or repro steps beyond the overlap). In the fork's current code both list tables are rendered with tableLayout="auto" and scroll={{ x: "max-content" }} … |
| [#287](https://github.com/Donkie/Spoolman/issues/287) | already-implemented | **Load Cell Actual Measured weight** — The exact capability requested -- feed a load cell's current gross weight reading and have Spoolman derive the actual remaining filament weight using the empty-spool weight -- already exists as a first-class API: PUT /api/v1/spool/{spool_id}/measure (spoolman/api/v1/spool.py:539-563, backed by spool… |
| [#290](https://github.com/Donkie/Spoolman/issues/290) | already-implemented | **Multiple Spool Count.** — The literal ask - collapsing N identical spools (same vendor/filament/color) into a single line-item that you check out, weigh, and check back in - does not exist as a distinct feature, and would actually conflict with the fork's per-spool tracking model, where each physical spool independently carr… |
| [#292](https://github.com/Donkie/Spoolman/issues/292) | already-implemented | **Move price & weight into spool** — This is already fully implemented in the fork's schema and UI. The Spool ORM model carries its own price (spoolman/database/models.py:73), initial_weight (models.py:76), and spool_weight (models.py:77), all stored independently of the parent Filament's values. |
| [#294](https://github.com/Donkie/Spoolman/issues/294) | already-implemented | **Labelprinter** — Both halves of this request are already covered by the fork. CSV/JSON export exists as a REST endpoint: GET /api/v1/export/spools\|filaments\|vendors accepts fmt=csv\|json (spoolman/api/v1/export.py:28-71) and flattens nested filament/vendor rows into dot-keyed CSV columns via dump_as_csv (spoolman/exp… |
| [#295](https://github.com/Donkie/Spoolman/issues/295) | out-of-scope | **Create slicer filament presets** — The fork's only export capability is the generic /api/v1/export/{spools,filaments,vendors} endpoint set (spoolman/api/v1/export.py:28-71), which dumps records as CSV or JSON via dump_as_csv/dump_as_json (invoked at export.py:81 and export.py:83); there is no PrusaSlicer/SuperSlicer/OrcaSlicer .ini o… |
| [#317](https://github.com/Donkie/Spoolman/issues/317) | already-implemented | **Spools Quantity** — The Spool create/clone form already has a dedicated Quantity stepper in its footer (client/src/pages/spools/create.tsx:258-268, backed by useState at line 182 and incrementQty/decrementQty at lines 183-189). On submit, if quantity > 1 the handler builds an array of `quantity` copies of the same form… |
| [#318](https://github.com/Donkie/Spoolman/issues/318) | already-implemented | **Subtype of filament** — The fork's generic Extra Fields system already lets an admin add exactly the kind of dedicated, filterable 'Subtype' field the issue asks for. ExtraFieldType includes a 'choice' type with an explicit choices list and an optional multi_choice flag (spoolman/extra_field_registry.py:26-40, :43-54), and… |
| [#344](https://github.com/Donkie/Spoolman/issues/344) | already-implemented | **Ability to sort spools by vendor?** — Sorting spools by vendor already works end to end. On the frontend, the Spool list's 'Filament' column (which shows the combined 'Vendor - Filament name' string) is created with `sorter: true` via the shared `commonProps` object (client/src/pages/spools/list.tsx:250-257, applied at 356-369), so clic… |
| [#353](https://github.com/Donkie/Spoolman/issues/353) | superseded by the fork's external filament catalog / 3D F… | **Separate list for generic filament types** — The Material field on the filament create/edit form is still a plain free-text Input with no predefined choices and no auto-filled density/diameter (client/src/pages/filaments/create.tsx:257-267): picking 'PLA' does nothing to the density/diameter fields by itself, so the literal ask (a customizable… |
| [#359](https://github.com/Donkie/Spoolman/issues/359) | already-implemented | **Allow a way to visualize multi color or color changing filament.** — Multi-color filament support is fully implemented end to end. The Filament model has color_hex plus multi_color_hexes/multi_color_direction fields (values coaxial or longitudinal), defined at spoolman/api/v1/models.py:96-100 and :161-185, and backed by DB migration migrations/versions/2024_05_28_184… |
| [#360](https://github.com/Donkie/Spoolman/issues/360) | already-implemented | **Quickly update location info from list of spools** — The fork ships a dedicated /locations route (client/src/App.tsx:146-147 and :228) that renders every location as a drop target (client/src/pages/locations/components/location.tsx) holding drag-and-drop SpoolCard tiles (client/src/pages/locations/components/spoolCard.tsx). Dropping a spool card onto … |
| [#379](https://github.com/Donkie/Spoolman/issues/379) | already-implemented | **Move the end-of-spool remainder of filament into another, dedicated spool** — There is no one-click merge-remainder-into-another-spool action, and the schema has no spool-lineage field (spoolman/database/models.py:66-87 defines Spool with no parent/merged-from column), so the exact single-operation workflow described does not exist. The underlying capability is nonetheless al… |
| [#395](https://github.com/Donkie/Spoolman/issues/395) | already-implemented | **Feature request - Add (SDS) Safety Data Sheet & Technical Data Sheet** — Spoolman has no file-upload or attachment storage anywhere in the backend — searching spoolman/ for UploadFile, multipart, or attachment returns no results — so the literal ask ('a way to easily upload files ... to a location') is not implemented and would require a new storage layer (path handling,… |
| [#404](https://github.com/Donkie/Spoolman/issues/404) | already-fixed | **Keep loosing access to spools** — The reported 500 was raised when the Spools list request carried sort=vendor.name:asc: parse_nested_field threw ValueError('Invalid field name vendor.name, vendor is not a valid field on Spool') because Spool has no direct vendor relationship (vendor lives on Filament). In the fork this ValueError n… |
| [#405](https://github.com/Donkie/Spoolman/issues/405) | environment-specific | **Filament not auto-updating in Fluidd/Mainsail when using 127.0.0.1** — The report states live weight updates stop in Fluidd/Mainsail only when Moonraker's [spoolman] server: is set to http://127.0.0.1:7912, while they work with the printer's real LAN IP and also work when opening Spoolman's own UI directly. Spoolman NG delivers its own live updates over a websocket (sp… |
| [#407](https://github.com/Donkie/Spoolman/issues/407) | environment-specific | **Application startup failed** — The crash originates from get_connection_url() at spoolman/database/database.py:36-38, which raises ValueError('Cannot specify a database name when using SQLite.') whenever SPOOLMAN_DB_TYPE=sqlite and SPOOLMAN_DB_NAME is also set. get_database() simply reads SPOOLMAN_DB_NAME from the environment (sp… |
| [#411](https://github.com/Donkie/Spoolman/issues/411) | out-of-scope | **Mobile octoprint, scanning qrcode to select filament** — The request is for QR-code-driven filament selection inside OctoPrint's own (mobile) web UI, which is code this repository does not own or ship — Spoolman cannot inject UI into another application's frontend. What Spoolman/the fork does provide is already substantial: a printable QR code per spool/f… |
| [#412](https://github.com/Donkie/Spoolman/issues/412) | already-fixed | **Permissions error after upgrading to 0.18.1** — The reported failure was that PUID/PGID were effectively ignored: the user set PUID=1001/PGID=1002 with host files owned 1001:1002, yet startup tried to chown to 1000:1001, failed with 'Operation not permitted', and env.check_write_permissions() called sys.exit(1). That exact path still exists (spoo… |
| [#413](https://github.com/Donkie/Spoolman/issues/413) | out-of-scope | **Issue with octoprint plugin, need serious help** — The issue is a JavaScript error that prevents selecting a spool in one of two OctoPrint instances managed via octoprint_deploy, and it occurs in the third-party OctoPrint-Spoolman plugin rather than in Spoolman itself; the reporter explicitly acknowledges 'I know this repo is not related to that ext… |
| [#415](https://github.com/Donkie/Spoolman/issues/415) | already-implemented | **Read Parameters from Spoolman into Printer** — Every value the reporter asks for can already be stored on a Filament or Spool and read back through the REST API, which is the only part of this request that is Spoolman's job (the printer-side macro plumbing is inherently the consumer's responsibility). Extruder and bed temperature are already nat… |
| [#419](https://github.com/Donkie/Spoolman/issues/419) | already-implemented | **Documentation and/or tutorial to explain the process of finding the density of a filament** — The upstream request asks for documentation or a calculator to help users find a filament's density when it isn't printed on the spool. The fork's actual answer to this problem is the filament catalog import: client/src/pages/filaments/create.tsx:125-126 renders an 'Import from external DB' button t… |
| [#420](https://github.com/Donkie/Spoolman/issues/420) | already-implemented | **Create Filament Swatch Card** — This is fully implemented, and covers the specific printables.com model the reporter linked ('Filament Swatch Card with Label'). Every row in the filament list has a 'download_swatch' action (client/src/pages/filaments/list.tsx:169-174) that opens SwatchDownloadModal (client/src/components/swatchDow… |
| [#427](https://github.com/Donkie/Spoolman/issues/427) | already-implemented | **selective search for extra fields** — The fork already supports filtering the spool/filament/vendor list endpoints by an extra-field value exactly as requested. The 'extra.' query-parameter prefix (EXTRA_FIELD_PREFIX = "extra.", spoolman/extra_field_registry.py:23) is read directly off request.query_params and forwarded as extra_field_f… |
| [#429](https://github.com/Donkie/Spoolman/issues/429) | environment-specific | **Sonic Pad** — The report asks Spoolman to be made to run on a Creality Sonic Pad, whose bundled Moonraker/OS the reporter says is too old to install Spoolman on at all. The repository has no Sonic-Pad- or vendor-firmware-specific code path; a search for 'sonic'/'creality'/'k1c' across the tree turns up no real hi… |
| [#437](https://github.com/Donkie/Spoolman/issues/437) | already-fixed | **New colour box makes black and white hard to see in dark and light mode** — The report asks for 'some sort of border' on the color swatch so that a pure-black spool is not lost against a dark-mode background and a pure-white spool is not lost against a light-mode background. The fork's swatch component now draws every color cell with a 2px border and rounded corners: .spool… |
| [#440](https://github.com/Donkie/Spoolman/issues/440) | already-implemented | **additional filament textures, glitter, shine, etc** — The Filament table has no dedicated texture/finish column (spoolman/database/models.py:31-58 lists name, material, color_hex, multi_color_hexes/direction, etc., but nothing like 'finish' or 'texture'), and SpoolIcon only ever paints flat color segments with no pattern/sparkle overlay (client/src/com… |
| [#441](https://github.com/Donkie/Spoolman/issues/441) | environment-specific | **Unable to run Spoolman in Docker Container on MacOS** — The reporter ran the Docker image on macOS 12.7.5 with a bind-mounted SQLite data dir (v0.19.3), saw a repeating startup error shown only in a screenshot (the image is not retrievable, so the exact message cannot be confirmed here), and reported that 'sudo chown -R 1000:1000 data' had no effect. Thi… |
| [#444](https://github.com/Donkie/Spoolman/issues/444) | out-of-scope | **Add additional color changing method** — The filament schema's only special-appearance fields today are color_hex and multi_color_hexes/multi_color_direction, and that direction enum is limited to coaxial and longitudinal (spoolman/api/v1/models.py:96-100, spoolman/externaldb.py:54-56); there is no thermochromic/photochromic (temperature o… |
| [#450](https://github.com/Donkie/Spoolman/issues/450) | out-of-scope | **Add air filter tracking** — Spoolman NG's stated scope is filament/spool inventory tracking - the README opens with 'Keep track of your inventory of 3D-printer filament spools' (README.md:9) - and every domain table in spoolman/database/models.py:15-123 models Vendor, Filament, Spool, and calibration sessions/steps; none of th… |
| [#452](https://github.com/Donkie/Spoolman/issues/452) | support-question | **Problems during installation in Dokker/Portainer** — This is an installation help request, not a defect report: the author states the Portainer image is created but shows as "unused" and asks "Can anyone help me how to install it?", plus a separate question about whether Spoolman can run on a BTT CB1 alongside Mainsail. No reproducible software fault,… |
| [#456](https://github.com/Donkie/Spoolman/issues/456) | already-implemented | **Datenbank Sicherung** — The report (German) asks whether the database can be backed up and moved to another Raspberry Pi. The fork already ships automatic nightly SQLite backups, enabled by default: spoolman/env.py:242-257 (`is_automatic_backup_enabled`) defaults `SPOOLMAN_AUTOMATIC_BACKUP` to TRUE, and spoolman/database/d… |
| [#466](https://github.com/Donkie/Spoolman/issues/466) | environment-specific | **Print Preview Renders Differently by Browser** — The label preview is plain HTML/CSS rendered directly by the browser: PrintingDialog builds the pages with millimetre-sized flex boxes and a system font stack (client/src/pages/printing/printingDialog.tsx:122-179 and the injected <style> at lines 247-285), then exports them with the html-to-image li… |
| [#489](https://github.com/Donkie/Spoolman/issues/489) | already-fixed | **Unable to leave price input field empty if focused once** — The spool create price field now uses parser={numberParserAllowEmpty} (client/src/pages/spools/create.tsx:341), and that parser returns an empty string rather than 0 when the input is empty (client/src/utils/parsing.tsx:86-99), so the price field can be focused and left blank without collapsing to 0… |
| [#490](https://github.com/Donkie/Spoolman/issues/490) | out-of-scope | **Helm Chart?** — There is no Helm chart or Kubernetes manifest anywhere in the repository (searches for '*helm*' and chart YAML files return nothing), and the documented deployment surface is exactly two paths: Docker Compose (docker-compose.yml:1-13, docs/installation.md:12-35) and a native Linux install via script… |
| [#494](https://github.com/Donkie/Spoolman/issues/494) | already-implemented | **Spool creation streamlining** — The core complaint, that vendor, filament, and spool require three separate manual steps, is now solved by the external filament catalog integration. The Spool create form's filament Select mixes internal filaments with roughly 6,957 catalog entries via useGetFilamentSelectOptions (client/src/pages/… |
| [#495](https://github.com/Donkie/Spoolman/issues/495) | already-implemented | **Import from External and Internal** — The exact merged search the reporter asked for already exists on the Spool creation/edit flow: useGetFilamentSelectOptions builds one Select whose options are grouped under an 'Internal' header and an 'External' header (client/src/pages/spools/functions.tsx:126-194), covering both the user's own alr… |
| [#498](https://github.com/Donkie/Spoolman/issues/498) | already-implemented | **Print on Label Printer** — The reporter's core complaint is that the only export path was a rasterized PNG, which their GODEX DT4X thermal printer would not accept, and their two suggested fixes were 'make it a PDF' or keep it a QR code + text on a plain white background. The current print dialog offers a native 'Print' butto… |
| [#504](https://github.com/Donkie/Spoolman/issues/504) | support-question | **spoolman don't connect with happy hare** — The log excerpt shows Happy Hare's mmu_server failing with 'HTTP error: 500 [Errno 111] Connection refused' when calling _get_spoolman_version(). Errno 111 (ECONNREFUSED) means nothing was listening at the host:port Happy Hare was configured to reach, i.e. |
| [#508](https://github.com/Donkie/Spoolman/issues/508) | insufficient-info | **Stopped Working** — Upstream report: after months of use the UI shows only a spinning icon with no version at the bottom and the user 'can't access the database', yet the pasted server log shows a fully healthy startup on v0.20.0 - migrations complete, external DB synced (2596 filaments, 23 materials), 'Startup complet… |
| [#510](https://github.com/Donkie/Spoolman/issues/510) | already-fixed | **Visual bug: title coloumns not scaling** — The reported symptom is extra-field column headers rendering one character per line (top-to-bottom) when many custom columns are shown, on upstream 0.20.0. In the fork the spool table is configured with tableLayout="auto" and scroll={{ x: "max-content" }} (client/src/pages/spools/list.tsx:332-333), … |
| [#522](https://github.com/Donkie/Spoolman/issues/522) | insufficient-info | **Spoolman no longer reachable after Ubuntu Upgrade to 24.04.1 LTS** — The report contains no error output, stack trace, or logs; it states only that Spoolman became unreachable in a browser after an in-place Ubuntu 22.04 to 24.04 upgrade, after which the reporter rolled back to a VM snapshot. There is no code path to investigate and no fork defect described. |
| [#524](https://github.com/Donkie/Spoolman/issues/524) | already-fixed | **Page doesn't render after adding an extra field with special characters** — In the current fork the extra-field unit is rendered only as plain text: the Settings extra-fields table column just prints it (client/src/pages/settings/extraFieldsSettings.tsx:474-477), the numeric display uses it as a trailing text node (client/src/components/numberField.tsx:24), and the create/e… |
| [#535](https://github.com/Donkie/Spoolman/issues/535) | already-implemented | **Add update git-repo style for Moonraker update service** — The request asks for Spoolman to be addable to Moonraker's update_manager so Klipper users (Mainsail/Fluidd) get one-click updates instead of manually re-running the install steps. This is already fully implemented: docs/installation.md:56-69 documents a ready-to-paste '[update_manager spoolman]' bl… |
| [#538](https://github.com/Donkie/Spoolman/issues/538) | already-fixed | **500 Internal Server Error** — The report is an intermittent 500 on 'GET /api/v1/spool?allow_archived=False' (~15x in 24h under a Home Assistant poll every 15 min) whose traceback fails inside SQLAlchemy connection checkout (spool.find -> db.execute -> pool.connect() -> pool/base.py), i.e. acquiring/validating a pooled DB connect… |
| [#543](https://github.com/Donkie/Spoolman/issues/543) | environment-specific | **Update Manager Moonraker Permission denied** — The report is a standalone v0.16.0 install on Raspbian whose Moonraker '[update_manager spoolman] type: web' update fails with 'permission denied'. With the 'web' update type Moonraker downloads a release zip and extracts it into the configured path (~/Spoolman); a 'permission denied' at that step i… |
| [#544](https://github.com/Donkie/Spoolman/issues/544) | already-fixed | **column width of the filament** — The filament list table now renders with tableLayout="auto" and scroll={{ x: "max-content" }} (client/src/pages/filaments/list.tsx:248-249). This explicitly overrides Ant Design's default table-layout:fixed that a scroll.x setting would otherwise impose, so under automatic layout the per-column widt… |
| [#550](https://github.com/Donkie/Spoolman/issues/550) | already-implemented | **Show printer name near spool** — Spoolman already has a generic, free-text `location` field on every spool that is designed for exactly this use case: the API's own example payload for GET /location lists `"Printer 1"`, `"Printer 2"`, `"Storage Shelf A"` as sample location names (spoolman/api/v1/other.py:112-115), showing the inten… |
| [#554](https://github.com/Donkie/Spoolman/issues/554) | already-fixed | **Incorrect handling of the decimal separator of a number when using a comma instead of a period.** — The report is that typing a comma-decimal like '0,3' in a float field turns into 3.0. In the current code the numeric inputs use comma-aware parsers defined in client/src/utils/parsing.tsx: numberParser (line 68) and numberParserAllowEmpty (line 86) both do value.replace(',', '.') (parsing.tsx:70 an… |
| [#556](https://github.com/Donkie/Spoolman/issues/556) | already-fixed | **invalid mount config for type "bind"** — Upstream report: `docker compose up` failed with `invalid mount config for type "bind": bind source path does not exist: .../data` because the compose file used the long-form `type: bind` volume syntax, which does not auto-create a missing host source directory; the reporter's workaround was `mkdir … |
| [#560](https://github.com/Donkie/Spoolman/issues/560) | already-implemented | **filament spool adjustment** — The exact capability requested - entering the current (gross) weight of a spool after weighing it, instead of manually computing and typing an amount used - already exists end to end. The backend exposes PUT /spool/{spool_id}/measure with `SpoolMeasureParameters.weight` documented as 'Current gross … |
| [#563](https://github.com/Donkie/Spoolman/issues/563) | out-of-scope | **Implementing Anycubic Kobra 3 goklipper** — This report is a request for community troubleshooting help getting Spoolman-related Klipper macros to run on Anycubic's proprietary 'goklipper' fork (github.com/ANYCUBIC-3D/Kobra3/tree/main/klipper-go) on a Kobra 3, where adding the macros to printer.cfg crashes that vendor firmware. There is no Kl… |
| [#564](https://github.com/Donkie/Spoolman/issues/564) | already-implemented | **Add API Route to add a new Location** — Location is not a first-class database entity in the fork; spoolman/database/spool.py:458 (find_locations) derives the list purely as `sqlalchemy.select(models.Spool.location).distinct()`, and spoolman/api/v1/other.py:102-126 exposes that as GET /api/v1/location, with a PATCH /api/v1/location/{locat… |
| [#568](https://github.com/Donkie/Spoolman/issues/568) | already-implemented | **Display HEX value on Filament Details page** — The request asks that a filament's color hex code be shown as an attribute on the filament details page (optionally also as a toggleable list column) so it can be copied into tools like Bambu Studio's color library. client/src/pages/filaments/show.tsx:115-117 already renders a 'Color Hex' title foll… |
| [#569](https://github.com/Donkie/Spoolman/issues/569) | already-implemented | **Automatically Calculate Remaining Weight when entering a spool** — The Spool creation form already has exactly this feature: a `weight_to_use` radio group offers `used_weight`, `remaining_weight`, and `measured_weight` modes (client/src/pages/spools/create.tsx:388-404; enum in client/src/pages/spools/model.tsx:3-7). Selecting "Measured Weight" - translated as "How … |
| [#573](https://github.com/Donkie/Spoolman/issues/573) | already-implemented | **Managing process parameters** — The fork's Calibration feature already provides essentially the 'Printer Object holding process parameters' the report asks for. A CalibrationSession is keyed by filament_id plus a free-text printer_name and nozzle_diameter (spoolman/database/models.py:90-107; spoolman/api/v1/calibration_models.py:7… |
| [#576](https://github.com/Donkie/Spoolman/issues/576) | already-fixed (same root cause as filament table header o… | **Table Header Overlap in German Language Version** — This is the same filament-list header overlap as issue 544, surfaced by longer German labels such as 'Düsentemperatur'. The current filament table uses tableLayout="auto" together with scroll={{ x: "max-content" }} (client/src/pages/filaments/list.tsx:248-249), which overrides Ant Design's default f… |
| [#580](https://github.com/Donkie/Spoolman/issues/580) | already-implemented | **Color and Shade Filament column breakout** — Neither `models.Filament` (spoolman/database/models.py:32-58) nor the client `IFilament` (client/src/pages/filaments/model.tsx) has a dedicated "primary color family" or "shade" column; only `color_hex`/`multi_color_hexes` (numeric) and the free-text `name` exist, so there is genuinely no automatic … |
| [#589](https://github.com/Donkie/Spoolman/issues/589) | environment-specific | **Help to fix please: Spoolman docker needs to get restart to start reading the filament from the printer** — The symptom ("log shows GET instead of PUT until Spoolman is restarted, then it starts PUTting usage") is governed entirely by the Moonraker spoolman client, not by Spoolman: Spoolman is a passive REST server that exposes PUT /spool/{id}/use (spoolman/api/v1/spool.py:502) and change-notification web… |
| [#592](https://github.com/Donkie/Spoolman/issues/592) | out-of-scope | **Add Purge Information to Spoolman** — There is no purge/waste concept anywhere in the fork: a repo-wide search for 'purge' across spoolman/, client/src, and docs returns zero hits, so this remains entirely unimplemented. The closest existing feature, calibration sessions, is strictly per-single-filament: spoolman/api/v1/calibration_mode… |
| [#599](https://github.com/Donkie/Spoolman/issues/599) | environment-specific | **Alpine linux support** — The request asks for Alpine Linux support, motivated by wanting to run Spoolman alongside Home Assistant (typically Home Assistant OS, an Alpine/OCI-based system). The production Dockerfile (Dockerfile:1, Dockerfile:41) builds on python:3.14-slim-trixie, i.e. |
| [#600](https://github.com/Donkie/Spoolman/issues/600) | already-implemented | **Sorting spool list by extra-field** — Both requested capabilities already exist in the fork's current code. Sorting by a custom extra-field is implemented end to end: client/src/components/column.tsx's CustomFieldColumn sets sorter: true and dataId: 'extra.<key>' for every extra field (column.tsx:565-577), spoolman/api/v1/spool.py forwa… |
| [#612](https://github.com/Donkie/Spoolman/issues/612) | already-fixed | **mmu gate field incorrectly displayed** — The MMU Gate column is a generic user/integration-created extra field (Happy Hare/Moonraker), not a fork-specific code path. Extra-field values are stored and validated as typed JSON on the backend (spoolman/extra_field_registry.py:62-112 requires json.loads to succeed and the decoded type to match … |
| [#613](https://github.com/Donkie/Spoolman/issues/613) | insufficient-info | **Column width is incorrect** — The filament overview now sets explicit widths on the temperature columns: settings_extruder_temp and settings_bed_temp are both declared with width 100 in client/src/pages/filaments/list.tsx:337-352, and the shared Column helper maps that value to antd's column width at client/src/components/column… |
| [#641](https://github.com/Donkie/Spoolman/issues/641) | already-implemented | **Spool Locations** — The underlying complaint - that 'location' wasn't useful, and there was no central place to see what spool is where - is substantially addressed by the fork's Locations board. client/src/pages/locations/components/locationContainer.tsx:38-71 groups all non-archived spools by their location field int… |
| [#643](https://github.com/Donkie/Spoolman/issues/643) | already-implemented | **Support storing Transmission Distance for filaments** — Storing a numeric 'Transmission Distance' (TD) value per filament is already possible today without any core changes: spoolman/extra_field_registry.py:32-40 defines an ExtraFieldType enum with `float` and `float_range` variants, and spoolman/extra_fields.py plus the settings-backed extra-field syste… |
| [#665](https://github.com/Donkie/Spoolman/issues/665) | already-fixed | **Spool weight** — The defect as reported - creating a spool does not pick up the filament's configured spool (tare) weight, forcing the user to look it up separately on the Filament page - is fixed in the current code. spoolman/database/spool.py:54-56 has the backend create() function default spool_weight from filame… |
| [#674](https://github.com/Donkie/Spoolman/issues/674) | already-fixed | **Layout CSS bug: Button icons should not wrap.** — Upstream report (v0.22.1): on the Locations page, when a location's label text exceeds ~25 characters the text and its trailing button icon wrap onto a second line; the icon should stay on the same line. The fork's Locations page has been reimplemented as a flexbox card grid: each location header is… |
| [#687](https://github.com/Donkie/Spoolman/issues/687) | already-fixed | **Support for Emoji in Label Templates** — The report claims emoji characters typed into a spool/filament label template print as literal question marks instead of the emoji glyph. The fork's shared print stylesheet already declares an emoji-capable font fallback chain on `.print-page` at client/src/pages/printing/printingDialog.tsx:275 ("..… |
| [#695](https://github.com/Donkie/Spoolman/issues/695) | already-implemented | **Mobile View** — The fork already carries deliberate, phone-width-targeted responsive design across the shell and key pages, contrary to the 'No UI for Mobile' claim. client/src/components/header/index.tsx:22-23 computes `isMobile` from `Grid.useBreakpoint()` and hides text labels (e.g. |
| [#698](https://github.com/Donkie/Spoolman/issues/698) | out-of-scope | **Option to change ID for spools** — The request asks for a way to set or change a spool's own ID, both so a freshly cloned replacement spool can reuse an old spool's ID (for a Klipper macro hardcoded to a numeric slot) and so spools can be manually sorted by ID. In this fork `Spool.id`/`Filament.id`/`Vendor.id` are plain autoincrement… |
| [#700](https://github.com/Donkie/Spoolman/issues/700) | superseded (by the external filament-catalog auto-create … | **Add spool/filament/manufacturer UX overhaul** — The concrete pain point - 'adding a spool of a filament/manufacturer I've never used before requires three separate screens' - is now solved differently but effectively via the fork's external filament database integration rather than the exact popup UI requested. client/src/pages/spools/functions.t… |
| [#708](https://github.com/Donkie/Spoolman/issues/708) | insufficient-info | **High CPU Usage** — The report provides only htop screenshots showing roughly 30% idle CPU (upstream v0.22.1, Docker on Debian, MariaDB) with no profiling to localize the hot path. Reviewing the current fork's recurring backend work, none of it explains sustained high idle CPU: Prometheus metric collection is the only … |
| [#709](https://github.com/Donkie/Spoolman/issues/709) | already-implemented | **Activate the spool through location** — Spoolman has no concept of an 'active spool' or per-tool/AMS-slot assignment at all; a Spool only stores a free-text location string (spoolman/database/models.py:79), and filament consumption is always recorded through PUT /spool/{spool_id}/use, which takes the numeric spool_id as a path parameter (… |
| [#716](https://github.com/Donkie/Spoolman/issues/716) | already-implemented | **Search spools in API and more by extra fields and/or add field for UID** — The reporter wants the spool search API to be filterable by extra-field values, e.g. `/api/v1/spool?extra.tag_uid=<uid>`, so external tools (their node-red/Home-Assistant flow) can look up a spool by a custom UID field instead of downloading and scanning every result. |
| [#723](https://github.com/Donkie/Spoolman/issues/723) | already-implemented | **Create NFC tags** — The fork already lets a user write spool data onto an NFC tag directly from the browser using the Web NFC API: client/src/components/nfcWriteModal.tsx:46-80 constructs a TigerTag payload and calls `new window.NDEFReader().write(...)`, which is exactly the Chrome Web NFC write flow the report links t… |
| [#724](https://github.com/Donkie/Spoolman/issues/724) | insufficient-info | **Starting problem after update** — The reporter upgraded from v0.18.1 to v0.22.1 on a Raspberry Pi Zero 2 W via `docker compose pull && up -d`, and the container never became reachable on its mapped port, but the report includes only the compose command output (which shows a normal pull/start) and no container logs, `docker logs` out… |
| [#737](https://github.com/Donkie/Spoolman/issues/737) | insufficient-info | **Spoolman Losing Data** — This is an unreproducible data-loss report against the old v0.22.1 (the reporter explicitly states Cannot reproduce), and no defect matching it is identifiable in the current fork code. The reporter's own screenshots are internally contradictory: the raw spool entity still holds its Location value w… |
| [#741](https://github.com/Donkie/Spoolman/issues/741) | already-implemented | **QR Code link to Filaments / Custom** — The request asks for QR-code label printing to work for filaments (not just spools), with the code linking to the filament's own page, because the reporter glues QR labels onto physical filament swatches rather than spools. This is already implemented: client/src/pages/filaments/show.tsx:75-90 adds … |
| [#744](https://github.com/Donkie/Spoolman/issues/744) | already-implemented | **Additional weight field** — The functional need described (fold an accessory's weight, e.g. a DryPod desiccant capsule, into the tare weight used for remaining-weight calculations) is already achievable: every Spool has its own overridable `spool_weight` field exposed on both the create and edit forms (client/src/pages/spools/… |
| [#745](https://github.com/Donkie/Spoolman/issues/745) | already-fixed | **Dockerfile size optimization** — The exact fix the reporter suggested is already in place: Dockerfile:1 and Dockerfile:41 build both the `python-builder` and `python-runner` stages FROM `python:3.14-slim-trixie`, a Debian slim base, not a full python image (the reporter explicitly asked for a `-bookworm-slim`-class image). The buil… |
| [#757](https://github.com/Donkie/Spoolman/issues/757) | insufficient-info | **Migration colors** — The request ('Fund this Site: filamentcolors.xyz ... Maybe it is possible to add these filaments and colors to spoolman Database') does not specify which fields, how many filaments, or what licensing/API terms would apply; a repo-wide case-insensitive search for 'filamentcolors' finds zero reference… |
| [#759](https://github.com/Donkie/Spoolman/issues/759) | superseded (fork's rewritten drag-orderable, modal-based … | **Location Tab not sorting in order** — The fork has rewritten location management, which supersedes both parts of this report. Location order is taken from the persisted 'locations' setting and is user-controllable via drag-and-drop rather than auto-sorted: locationsList is assembled in client/src/pages/locations/components/locationConta… |
| [#764](https://github.com/Donkie/Spoolman/issues/764) | out-of-scope | **manual extrusion not recorded.** — Spoolman is a passive REST server with no connection to the printer; it can only record consumption that an external integration explicitly reports via POST /api/v1/spool/{spool_id}/use (spoolman/api/v1/spool.py:502-535, accepting use_length or use_weight per spool.py:106-107). Manual extrusion thro… |
| [#767](https://github.com/Donkie/Spoolman/issues/767) | support-question | **Spoolman wont load front end.** — This report has no code-level reproduction detail: the user says the standalone app loads fine in a browser at their Pi's own address but does not 'show up in mainsail so there is no way to select the roll of filament,' with no Spoolman version, no server logs, and no moonraker.conf shown. Spoolman … |
| [#772](https://github.com/Donkie/Spoolman/issues/772) | already-implemented | **Expose base URL in spoolman API** — The fork already exposes exactly this: a user-configurable 'base_url' setting is registered as a plain string setting (spoolman/settings.py:76) and is readable through the generic GET /api/v1/setting/{key} endpoint (spoolman/api/v1/setting.py:47-62), so any external tool, including a Moonraker compo… |
| [#776](https://github.com/Donkie/Spoolman/issues/776) | already-implemented | **Support for OpenPrintTag** — The fork has a dedicated OpenPrintTag implementation. spoolman/openprinttag_codec.py:1-78 is an NFC-V (ISO 15693) NDEF/CBOR decoder explicitly built against the OpenPrintTag spec ("Based on the OpenPrintTag specification: https://specs.openprinttag.org", line 7), including the material-type/material… |
| [#781](https://github.com/Donkie/Spoolman/issues/781) | already-implemented | **Please add documentation on how to configure the Spoolman service** — Upstream requested documentation on how the Spoolman service is configured, which files are involved, and how to customize them, having found only source diving as an option. The fork's docs/installation.md is a full configuration guide covering Docker and native Linux installs, a complete environme… |
| [#788](https://github.com/Donkie/Spoolman/issues/788) | environment-specific | **[Integration Issue] Consumption Data (POST /use/filament) Not Received from OctoEverywhere API Tunnel** — Spoolman exposes the consumption endpoint the integration needs: usage is applied via PUT /api/v1/spool/{spool_id}/use, implemented at spoolman/api/v1/spool.py:502-535 (it reduces the spool by use_weight or use_length). The reporter's own log analysis states that no usage request ever reaches Spoolm… |
| [#796](https://github.com/Donkie/Spoolman/issues/796) | support-question | **this project is still alive?** — This is a one-line question about upstream Donkie/Spoolman's maintenance status, not a bug report or feature request, and there is no code path to investigate. It is factually superseded from the fork's perspective: CHANGELOG.md:1-13 documents “Spoolman NG” as a community-maintained continuation of … |
| [#799](https://github.com/Donkie/Spoolman/issues/799) | already-implemented | **RFID** — The fork already implements the exact workflow requested: a per-spool "Write NFC" button (client/src/pages/spools/show.tsx:153-160) opens a modal with a format picklist — `Segmented` options for "TigerTag (NTAG213)" vs "Qidi (MIFARE Classic)" (client/src/components/nfcWriteModal.tsx:140-163) — and w… |
| [#813](https://github.com/Donkie/Spoolman/issues/813) | already-implemented | **Add Spool Type object holding the weight of an empty spool** — Upstream asked for a reusable 'Spool Type' object holding an empty-spool weight because, in their experience, editing a filament's spool weight was overwriting the manually-set weight on already-created spools. In the fork's schema, Spool has its own persisted spool_weight column (spoolman/database/… |
| [#824](https://github.com/Donkie/Spoolman/issues/824) | already-implemented | **Ability to print QR-codes for Filament aswell as Spool** — This exact capability is implemented: the filaments list toolbar has a print button that calls `navigate('print')` (client/src/pages/filaments/list.tsx:193) labelled with `t('printing.qrcode.button')` (line 196), routed by `client/src/App.tsx:211` (`<Route path='print' element={<LoadablePage name='f… |
| [#828](https://github.com/Donkie/Spoolman/issues/828) | already-implemented | **Generate Json Data from a Spool for writing onto a NFC Tag** — Both branches of the request are implemented. Direct browser writing (the reporter doubted this was "possible from a web app") exists via the Web NFC API: client/src/components/nfcWriteModal.tsx:46-80 builds a TigerTag payload and calls `NDEFReader.write(...)` directly from the page, no separate app… |
| [#830](https://github.com/Donkie/Spoolman/issues/830) | already-fixed | **Moonraker warning - update_manager after upgrade** — The exact warnings quoted (missing requirements.txt, unparsed repo/persistent_files/managed_services options) come from configuring Moonraker's update_manager with the old `type: git_repo` method, which needs a requirements.txt at the repository root; this checkout has no requirements.txt anywhere a… |
| [#833](https://github.com/Donkie/Spoolman/issues/833) | out-of-scope | **Feature Request: Add Kexcelled to external filaments list** — Spoolman NG does not store the filament/vendor catalog in this repository at all: spoolman/externaldb.py:22 sets DEFAULT_EXTERNAL_DB_URL to the fork's own community-maintained data repo (https://sherrmann.github.io/SpoolmanDB/), and get_external_db_url() at spoolman/externaldb.py:129-131 lets operat… |
| [#854](https://github.com/Donkie/Spoolman/issues/854) | insufficient-info | **Export spool library to Hueforge JSON format** — Upstream wants an export mode that dumps the spool/filament library as JSON directly importable into the third-party tool HueForge, with an option to pick which custom/extra field supplies the 'TD' (transmission distance) value used for multi-color print profiles. The fork's export system currently … |
| [#881](https://github.com/Donkie/Spoolman/issues/881) | already-fixed | **Standalone installation fails in Debian 12/13** — The reported failure was 'uvicorn: command not found' (systemd exit 127) when scripts/start.sh ran after a standalone install on Debian/DietPi, i.e. uvicorn was never installed into the venv. |
| [#900](https://github.com/Donkie/Spoolman/issues/900) | out-of-scope | **Color should be definable with the Spool and not the Filament** — In the fork's current schema, color_hex, multi_color_hexes, and multi_color_direction all live on Filament (spoolman/database/models.py:51-53) and Spool (models.py:66-88) has no color column at all, so the literal request (move color to the Spool) is indeed unimplemented. However this contradicts a … |
| [#911](https://github.com/Donkie/Spoolman/issues/911) | already-implemented | **Import from 3D Filament Profile Database** — The fork already implements exactly this: spoolman/api/v1/externaldb.py:90-118 exposes GET /api/v1/external/profile/{profile_id}, which fetches https://3dfilamentprofiles.com/filament/details/{id} (base URL overridable via EXTERNAL_3DFP_URL, externaldb.py:27-36) and spoolman/api/v1/externaldb.py:121… |
| [#918](https://github.com/Donkie/Spoolman/issues/918) | already-implemented | **Lost Database Twice** — The reporter lost their SQLite database twice after updating Spoolman on a Raspberry Pi (alongside a third-party CFSync add-on) and asks for 'a better backup and update method' with an easy export/restore feature. That capability now exists in this fork: `SPOOLMAN_AUTOMATIC_BACKUP` defaults to TRUE … |
| [#926](https://github.com/Donkie/Spoolman/issues/926) | out-of-scope | **Remaining weight is not correct after printing coloured objects** — Spoolman's own consumption path is a thin, generic counter: `PUT /spool/{id}/use` (spoolman/api/v1/spool.py:501-536) takes only `use_weight` or `use_length` (SpoolUseParameters, spool.py:105-107) and applies exactly that delta via `use_weight_safe()` (spoolman/database/spool.py:270-313), a single at… |
| [#939](https://github.com/Donkie/Spoolman/issues/939) | already-implemented | **Fetch filament info from https://3dfilamentprofiles.com/** — This is already shipped in the fork. spoolman/api/v1/externaldb.py:27 defines DEFAULT_3DFP_BASE_URL = 'https://3dfilamentprofiles.com', and get_3dfp_base_url() (externaldb.py:30-36) is overridable via the EXTERNAL_3DFP_URL env var, documented in .env.example:65-66. |
| [#944](https://github.com/Donkie/Spoolman/issues/944) | out-of-scope | **loading info into lane_data** — The report is a show-and-tell about an external community Klipper plugin (broncosis/spoolman-lane-sync) that reads Spoolman data to sync color/type into AFC/Happy Hare `lane_data` for OrcaSlicer, deliberately implemented outside Spoolman ("spoolman doesn't have to run on the klipper instance"). A se… |
| [#945](https://github.com/Donkie/Spoolman/issues/945) | out-of-scope | **external fillament to add** — The report itself is minimal: five vendor names (Creality, eSun, Bambu, Sunlu, 'Siddament') plus an attached filaments.json, with zero reactions and zero comments. The filament/vendor catalog data does not live in this repository at all: spoolman/externaldb.py only fetches and parses a catalog from … |
| [#949](https://github.com/Donkie/Spoolman/issues/949) | support-question | **SpoolmanDB-Community** — The report (“how do I use SpoolmanDB-Community, I tried the API field but nothing changed”) is a how-to question rather than a reproducible defect, and the mechanism it is asking about already exists and is already documented, just not as an “API field”. The filament catalog source is controlled exc… |

---
*Full per-issue verdicts (findings, code references, confidence) are embedded in the filed fork issues; the raw upstream dump is `upstream_issues.json` at the repo root (committed on master).*
